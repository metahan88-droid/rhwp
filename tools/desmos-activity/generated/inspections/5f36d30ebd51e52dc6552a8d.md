# Amplify Activity Inspection

Source URL: https://classroom.amplify.com/activity/5f36d30ebd51e52dc6552a8d?lang=ko

## Static Metadata

- Activity ID: 5f36d30ebd51e52dc6552a8d
- Static title: 점 수집: 직선
- Description: 학생은 이 액티비티에서 1차 부등식을 이용해 좌표의 점을 모으는 놀이를 통해 1차 부등식의 심화된 개념을 학습할 수 있습니다.

이 액티비티는 여러 번의 수업에 걸쳐 진행해야 할 수도 있습니다. 액티비티를 나중에 이어서 진행하려면 학생이 로그인하도록 지도해 주세요.

한국어 번역: Desmos 현지화 팀
- Canonical: https://classroom.amplify.com/activity/5f36d30ebd51e52dc6552a8d
- Image: https://uploads.desmos.com/activitybuilder/e62e7181c9e4476c56d84f3d294673d2

## Activity JSON Summary

- Title: 점 수집: 직선
- Version: 1
- Commit ID: 62ff3fdccee337d662e3f094
- Published: 2022-08-19T08:16:34.533Z
- Edited: 2022-08-19T08:16:34.533Z
- Permission to share: true
- Noindex: false
- Screen count: 13

## Component Counts

- exhibit/text: 14
- input/graph: 11
- input/expression: 10
- card: 4
- sketch: 2
- background: 2
- multiple-choice: 2
- 4ce38269-9915-4239-9a97-aa50fb5444de: 1
- 003e99a2-8502-41dc-8a7c-6e0b09967536: 1
- 6036aef8-172e-4a0b-8d1f-e7f5dcca07fd: 1
- 94ae2455-ffea-4a18-b785-7847c2a064d9: 1
- b028a78f-5dc1-4dbb-ac56-68badf9d7910: 1
- b8055517-0073-41d7-adca-efc207751e8b: 1
- challenge: 1
- action-button: 1

## Screens

| # | Title | Type | Components |
|---:|---|---|---|
| 1 | 챌린지 준비 1 | two-column | exhibit/text:1, input/graph:1 |
| 2 | 챌린지 준비 2 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 3 | 챌린지 준비 3 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 4 | 챌린지 준비 4 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 5 | 1번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 6 | 2번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 7 | 3번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 8 | 4번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 9 | 부등식 맞추기 | two-column | 003e99a2-8502-41dc-8a7c-6e0b09967536:1, 4ce38269-9915-4239-9a97-aa50fb5444de:1, background:1, exhibit/text:1, multiple-choice:1, sketch:1 |
| 10 | 부등식 예측하기 | two-column | background:1, exhibit/text:1, input/expression:1, sketch:1 |
| 11 | 다른 부등식 찾기 | two-column | 6036aef8-172e-4a0b-8d1f-e7f5dcca07fd:1, 94ae2455-ffea-4a18-b785-7847c2a064d9:1, b028a78f-5dc1-4dbb-ac56-68badf9d7910:1, b8055517-0073-41d7-adca-efc207751e8b:1, card:4, exhibit/text:1, multiple-choice:1 |
| 12 | 5번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 13 | (untitled) | two-column | action-button:1, challenge:1, exhibit/text:2, input/expression:1, input/graph:2 |

## CL Scripts

## Script 1: Screen 1 w1graph (input/graph)

```cl
numberList("S"):aggregate(w1graph.number("s"))

correct: w1graph.number("I") = 1
```

## Script 2: Screen 1 w1note (exhibit/text)

```cl
b = firstDefinedValue(w1graph.number("b_1"), 0)
r = firstDefinedValue(w1graph.number("r_1"), 0)
score = firstDefinedValue(w1graph.number("s"), 0)

countBetter = w1graph.number("m")

line1 = "점을 모아서 최대한 높은 점수를 획득해 보세요."

redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b = 1 "파란 점 1개"
  otherwise "파란 점 ${b}개"

line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다.

(힌트: 보라색 점을 드래그해 보세요.)"

feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"
```

## Script 3: Screen 2 w2graph (input/graph)

```cl
function("g"): simpleFunction("g\\left(x,y\\right)=\\left\\{${w2input.latex}:1,\ 0\\right\\}","x","y")
numberList("S"):aggregate(w2graph.number("s"))

readOnly: true
```

## Script 4: Screen 2 w2note (exhibit/text)

```cl
b = firstDefinedValue(w2graph.number("b_1"), 0)
r = firstDefinedValue(w2graph.number("r_1"), 0)
score = firstDefinedValue(w2graph.number("s"), 0)

countBetter = w2graph.number("m")

line1 = "이제 부등식을 이용해 점을 모아 보겠습니다. 부등식을 수정해 최대한 높은 점수를 획득해 보세요."

redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b = 1 "파란 점 1개"
  otherwise "파란 점 ${b}개"

line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다.

(힌트: 부등식의 방향을 바꿔 보세요.)"

feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"
```

## Script 5: Screen 2 w2input (input/expression)

```cl
initialLatex: "y < x"

correct: w2graph.number("I") = 1
```

## Script 6: Screen 3 w3graph (input/graph)

```cl
function("g"): simpleFunction("g\\left(x,y\\right)=\\left\\{${w3input.latex}:1,\ 0\\right\\}","x","y")
numberList("S"):aggregate(w3graph.number("s"))

readOnly: true
```

## Script 7: Screen 3 w3note (exhibit/text)

```cl
b = firstDefinedValue(w3graph.number("b_1"), 0)
r = firstDefinedValue(w3graph.number("r_1"), 0)
score = firstDefinedValue(w3graph.number("s"), 0)

countBetter = w3graph.number("m")

line1 = "부등식을 수정해 최대한 높은 점수를 획득해 보세요."

redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b = 1 "파란 점 1개"
  otherwise "파란 점 ${b}개"

line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다.

(힌트: 기울기를 수정해 보세요.)"

feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"
```

## Script 8: Screen 3 w3input (input/expression)

```cl
initialLatex: "y > 2x"

correct: w3graph.number("I") = 1
```

## Script 9: Screen 4 w4graph (input/graph)

```cl
function("g"): simpleFunction("g\\left(x,y\\right)=\\left\\{${w4input.latex}:1,\ 0\\right\\}","x","y")
numberList("S"):aggregate(w4graph.number("s"))

readOnly: true
```

## Script 10: Screen 4 w4note (exhibit/text)

```cl
b = firstDefinedValue(w4graph.number("b_1"), 0)
r = firstDefinedValue(w4graph.number("r_1"), 0)
score = firstDefinedValue(w4graph.number("s"), 0)

countBetter = w4graph.number("m")

line1 = "부등식을 수정해 최대한 높은 점수를 획득해 보세요."

redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b = 1 "파란 점 1개"
  otherwise "파란 점 ${b}개"

line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다.

(힌트: y 절편을 수정해 보세요.)"

feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"
```

## Script 11: Screen 4 w4input (input/expression)

```cl
initialLatex: "y \\ge \\frac{1}{2} x + 1"

correct: w4graph.number("I") = 1
```

## Script 12: Screen 5 c1graph (input/graph)

```cl
function("g"): simpleFunction("g\\left(x,y\\right)=\\left\\{${c1input.latex}:1,\ 0\\right\\}","x","y")
numberList("S"):aggregate(c1graph.number("s"))

readOnly: true
```

## Script 13: Screen 5 c1note (exhibit/text)

```cl
b = firstDefinedValue(c1graph.number("b_1"), 0)
r = firstDefinedValue(c1graph.number("r_1"), 0)
score = firstDefinedValue(c1graph.number("s"), 0)

countBetter = c1graph.number("m")

line1 = "부등식을 입력해 점을 모아야 합니다. 최대한 높은 점수를 획득해 보세요."
redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b=1 "파란 점 1개"
  otherwise "파란 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다."
    
feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"
```

## Script 14: Screen 5 c1input (input/expression)

```cl
correct: c1graph.number("I") = 1
```

## Script 15: Screen 6 c2graph (input/graph)

```cl
function("g"): simpleFunction("g\\left(x,y\\right)=\\left\\{${c2input.latex}:1,\ 0\\right\\}","x","y")
numberList("S"):aggregate(c2graph.number("s"))

readOnly: true
```

## Script 16: Screen 6 c2note (exhibit/text)

```cl
b = firstDefinedValue(c2graph.number("b_1"), 0)
r = firstDefinedValue(c2graph.number("r_1"), 0)
score = firstDefinedValue(c2graph.number("s"), 0)

countBetter = c2graph.number("m")

line1 = "부등식을 입력해 점을 모아야 합니다. 최대한 높은 점수를 획득해 보세요."
redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b=1 "파란 점 1개"
  otherwise "파란 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다."
    
feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"
```

## Script 17: Screen 6 c2input (input/expression)

```cl
correct: c2graph.number("I") = 1
```

## Script 18: Screen 7 c3graph (input/graph)

```cl
function("g"): simpleFunction("g\\left(x,y\\right)=\\left\\{${c3input.latex}:1,\ 0\\right\\}","x","y")
numberList("S"):aggregate(c3graph.number("s"))

readOnly: true
```

## Script 19: Screen 7 c3note (exhibit/text)

```cl
b = firstDefinedValue(c3graph.number("b_1"), 0)
r = firstDefinedValue(c3graph.number("r_1"), 0)
score = firstDefinedValue(c3graph.number("s"), 0)

countBetter = c3graph.number("m")

line1 = "부등식을 입력해 점을 모아야 합니다. 최대한 높은 점수를 획득해 보세요."
redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b=1 "파란 점 1개"
  otherwise "파란 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다."
    
feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"

```

## Script 20: Screen 7 c3input (input/expression)

```cl
correct: c3graph.number("I") = 1
```

## Script 21: Screen 8 c4graph (input/graph)

```cl
function("g"): simpleFunction("g\\left(x,y\\right)=\\left\\{${c4input.latex}:1,\ 0\\right\\}","x","y")
numberList("S"):aggregate(c4graph.number("s"))

readOnly: true
```

## Script 22: Screen 8 c4note (exhibit/text)

```cl
b = firstDefinedValue(c4graph.number("b_1"), 0)
r = firstDefinedValue(c4graph.number("r_1"), 0)
score = firstDefinedValue(c4graph.number("s"), 0)

countBetter = c4graph.number("m")

line1 = "부등식을 입력해 점을 모아아 합니다. 최대한 높은 점수를 획득해 보세요."
redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b=1 "파란 점 1개"
  otherwise "파란 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다."
    
feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"

```

## Script 23: Screen 8 c4input (input/expression)

```cl
correct: c4graph.number("I") = 1
```

## Script 24: Screen 10 4f7e05d0-e98a-4c3c-b90d-1eb84ed5b219 (sketch)

```cl
readOnly: true
```

## Script 25: Screen 10 input10 (input/expression)

```cl
explainPrompt: "답변을 설명해 주세요. 그림판 툴을 이용해 설명해도 좋습니다."

correct: input10.numericValue = 3
```

## Script 26: Screen 12 e1graph (input/graph)

```cl
function("g"): simpleFunction("g\\left(x,y\\right)=\\left\\{${e1input.latex}:1,\ 0\\right\\}","x","y")
numberList("S"):aggregate(e1graph.number("s"))

readOnly: true
```

## Script 27: Screen 12 e1note (exhibit/text)

```cl
b = firstDefinedValue(e1graph.number("b_1"), 0)
r = firstDefinedValue(e1graph.number("r_1"), 0)
score = firstDefinedValue(e1graph.number("s"), 0)

countBetter = e1graph.number("m")

line1 = "부등식을 입력해 점을 모아아 합니다. 최대한 높은 점수를 획득해 보세요."
redMsg =
  when r = 1 "빨간 점 1개"
  otherwise "빨간 점 ${r}개"
blueMsg =
  when b=1 "파란 점 1개"
  otherwise "파란 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수는 ${score}점입니다.

(힌트: 'x - 1 < y < x + 1'과 같이 입력해 보세요.)"
    
feedback =
    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${feedback}"
```

## Script 28: Screen 12 e1input (input/expression)

```cl
correct: e1graph.number("I") = 1
```

## Script 29: Screen 13 ca4eb99b-c608-458e-bda4-e6e6b39b8d2e (challenge)

```cl
totalscore = firstDefinedValue(ccgraph2.number("s"), 0)
bscore = firstDefinedValue(ccgraph2.number("b_1"), 0)
bplaced = ccgraph1.number("b_c")
buttonOn = ccgraph1.number("S") = 1

challengeBackground: graphLayer(ccgraph2.calculatorState)
challengeBounds: ccgraph2.bounds

responseBackground: graphLayer(ccgraph2.calculatorState)
responseBounds: ccgraph2.bounds

preventSubmissionReason:

	when bplaced = 0 "챌린지를 제출하려면 이전 화면에 최소 1개의 파란색 점이 있어야 합니다."
    when not(buttonOn) "챌린지를 제출하려면 이전 화면의 점을 고정해야 합니다."
#   when not(bplaced = totalscore and bscore > 0) "챌린지를 제출하려면 모든 파란색 점을 수집해야 하며, 빨간색 점이 하나도 포함되어 있지 않아야 합니다."
    when not(totalscore > 0) "챌린지를 제출하려면 획득한 점수가 양수여야 합니다."
    otherwise ""
```

## Script 30: Screen 13 ccgraph1 (input/graph)

```cl
number("P_c"): ccbutton1.pressCount

numberList("V_b"): ccgraph1.numberList("V_{b0}")
numberList("V_r"): ccgraph1.numberList("V_{r0}")
```

## Script 31: Screen 13 68d8312a-04b2-4777-8f1d-7613d0a25283 (exhibit/text)

```cl
message1 = "자신만의 점 수집 챌린지를 만들어 보세요. 파란색과 빨간색 상자에서 점을 드래그해서 점을 표시하세요.

점을 표시했으면 <점 고정> 버튼을 클릭하세요."

message2 = "챌린지를 다 만들었다면 다음 화면으로 이동해 주세요.

챌린지를 수정하고 싶다면 <점 고정 해제> 버튼을 클릭하세요."

content:
	when (ccgraph1.number("S") = 0) "${message1}"
    otherwise "${message2}"
```

## Script 32: Screen 13 ccbutton1 (action-button)

```cl
label: when (ccgraph1.number("S") = 0) "점 고정" otherwise "점 고정 해제"
```

## Script 33: Screen 13 ccgraph2 (input/graph)

```cl
numberList("A"): ccgraph1.numberList("A")
numberList("B"): ccgraph1.numberList("B")
numberList("C"): ccgraph1.numberList("C")
numberList("D"): ccgraph1.numberList("D")

numberList("I_b"): ccgraph1.numberList("I_b")
numberList("I_r"): ccgraph1.numberList("I_r")

number("I"): ccgraph1.number("S")

#rawExpression("g"): "g\\left(x,y,z\\right)=\\left\\{${ccinput2.latex}:1,\ 0\\right\\}"

#rawExpression("g"): "g\\left(x,y,z\\right)=\\left\\{${ccinput2.latex}:\\left\\{z=0:1,0\\right\\},0\\right\\}"

#rawExpression("g"): "g\\left(x,y\\right)=\\left\\{${ccinput2.latex}:1,0\\right\\}"
function("g"): simpleFunction("\\left\\{${ccinput2.latex}:1,0\\right\\}","x","y")
#numberList("S"):aggregate(ccgraph2.number("s"))
```

## Script 34: Screen 13 ea191624-cec0-440f-baf6-79156bd5abaf (exhibit/text)

```cl
b = firstDefinedValue(ccgraph2.number("b_1"), 0)
r = firstDefinedValue(ccgraph2.number("r_1"), 0)
score = firstDefinedValue(ccgraph2.number("s"), 0)

#countBetter = ccgraph2.number("m")

line1 = "부등식을 이용해 점을 모아 보세요. 최대한 높은 점수를 획득해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b = 1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 모았습니다.

현재 점수: ${score}"

#feedback =
#    when (countBetter = 0) and (score > 0) "반에서 최고 점수를 획득했습니다."
#    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
#    otherwise ""

#content: "${line1}\n\n${line2}\n\n${feedback}"

content: "${line1}\n\n${line2}"
```

## Script 35: Screen 13 ccinput2 (input/expression)

```cl
correct: ccgraph2.number("I_c") = 1

#warning: when not(ccgraph2.number("I_c") = 1) "챌린지를 제출하려면 획득한 점수가 양수여야 합니다." otherwise ""
```

