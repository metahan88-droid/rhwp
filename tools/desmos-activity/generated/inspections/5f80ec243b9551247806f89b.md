# Amplify Activity Inspection

Source URL: https://classroom.amplify.com/activity/5f80ec243b9551247806f89b?lang=ko

## Static Metadata

- Activity ID: 5f80ec243b9551247806f89b
- Static title: 페인트칠하기
- Description: 학생은 이 액티비티에서 페인트를 만들고 칠하는 과정을 통해 분수와 비율에 대해 배울 수 있습니다. 

학생은 비율표를 이용해 페인트를 섞어 주어진 색의 페인트를 만들어야 합니다. 또한 주어진 색의 페인트를 만들려면 목록에서 어떤 페인트 조합을 선택해야 하는지도 결정해야 합니다.

이 액티비티는 Illustrative Mathematics와 Open Up Resources의 도움을 받아 제작되었습니다. https://openupresources.org에서 자료를 무료로 다운로드 받을 수 있습니다.

한국어 번역: Desmos 현지화 팀
- Canonical: https://classroom.amplify.com/activity/5f80ec243b9551247806f89b
- Image: https://uploads.desmos.com/activitybuilder/46c86f43fcac167c2489e709be2cf12d

## Activity JSON Summary

- Title: 페인트칠하기
- Version: 1
- Commit ID: 630348cb72fd4aeea7164f67
- Published: 2022-08-22T09:53:31.785Z
- Edited: 2022-08-22T09:53:31.785Z
- Permission to share: true
- Noindex: false
- Screen count: 6

## Component Counts

- table: 12
- exhibit/text: 8
- input/graph: 7
- action-button: 4
- sketch: 1
- multiple-choice: 1
- df08ad2a-1038-4ca9-945c-c05e475b5a40: 1
- 04a8f399-f6ae-4e9e-bbd1-4fff1c645037: 1
- f452896a-345e-4acf-8741-2d4efc330aea: 1
- c-2: 1
- challenge: 1

## Screens

| # | Title | Type | Components |
|---:|---|---|---|
| 1 | 액티비티 준비 | two-column | exhibit/text:1, input/graph:1 |
| 2 | 같은 색의 페인트 만들기 | two-column | action-button:1, exhibit/text:1, input/graph:1, table:1 |
| 3 | 생각해 보기 | two-column | 04a8f399-f6ae-4e9e-bbd1-4fff1c645037:1, c-2:1, df08ad2a-1038-4ca9-945c-c05e475b5a40:1, exhibit/text:1, f452896a-345e-4acf-8741-2d4efc330aea:1, multiple-choice:1, sketch:1 |
| 4 | 마이클 도와주기 | two-column | action-button:1, exhibit/text:1, input/graph:1, table:1 |
| 5 | 라라 도와주기 | two-column | action-button:1, exhibit/text:1, input/graph:1, table:1 |
| 6 | (untitled) | two-column | action-button:1, challenge:1, exhibit/text:3, input/graph:3, table:9 |

## CL Scripts

## Script 1: Screen 1 graphIntro1 (input/graph)

```cl
trace: false
bounds: makeBounds(this.number("x_m"), this.number("x_M"), this.number("y_m"), this.number("y_M"))

export("white cups"): "${this.number("w_{hiteCups}")}"
export("green cups"): "${this.number("g_{reenCups}")}"

narration(1): "흰색과 초록색 페인트를 섞어서 만든 페인트를 담은 페인트통이 표시되어 있습니다. 움직일 수 있는 점 하나는 흰색 페인트통의 수를 조절하고, 다른 움직일 수 있는 점 하나는 초록색 페인트통의 수를 조절합니다."
narration(2): "흰색 페인트가 ${whitecups}통 있습니다."
narration(3): "초록색 페인트가 ${greencups}통 있습니다."
whitecups = this.number("w_{hiteCups}")
isarewhitecups = when whitecups=1 "" otherwise ""
swhitecups = when whitecups=1 "" otherwise ""
greencups = this.number("g_{reenCups}")
isaregreencups = when greencups=1 "" otherwise ""
sgreencups = when greencups=1 "" otherwise ""
```

## Script 2: Screen 2 graph2 (input/graph)

```cl
b = button2
g = graphIntro1
in = inputCheck

trace: false
readOnly: true

bounds: makeBounds(this.number("x_m"), this.number("x_M"), this.number("y_m"), this.number("y_M"))

number("t_0"): b.timeSincePress() 
number("g_{reenGuess}"): 
  when firstDefinedValue(in.cellNumericValue(2,2),1) <0   0 
  when firstDefinedValue(in.cellNumericValue(2,2),1) >100 100 
  otherwise firstDefinedValue(in.cellNumericValue(2,2),0)

number("g_{reenUncapped}"): 
  when firstDefinedValue(in.cellNumericValue(2,2),1) <0 0 
  otherwise firstDefinedValue(in.cellNumericValue(2,2),0)

usePrevious = when g.number("w_{hiteCups}") = g.number("g_{reenCups}") false otherwise true

number("w_{hiteCupsPrevious}"):  when usePrevious g.number("w_{hiteCups}") otherwise 2
number("g_{reenCupsPrevious}"):  when usePrevious g.number("g_{reenCups}") otherwise 3

narration(1): "그림에는 페인트를 만들기 위해 ${who}가 사용한 흰색 페인트 ${topwhitecups}통과 초록색 페인트 ${topgreencups}통이 표시되어 있습니다. 추가로 흰색 페인트 ${bottomwhitecups}통도 표시되어 있습니다."
narration(2): 
  when time>0 "초록색 페인트 ${bottomgreencups}통과 흰색 페인트 ${bottomwhitecups}통을 추가로 섞었습니다."
  otherwise ""
narration(3):
  when over "${success} 새로 만든 페인트의 색이 ${compare}"
  otherwise ""
topwhitecups = when usePrevious g.number("w_{hiteCups}") otherwise 2
stopwhitecups = when topwhitecups=1 "" otherwise ""
topgreencups = when usePrevious g.number("g_{reenCups}") otherwise 3
stopgreencups = when topgreencups=1 "" otherwise ""
who = when usePrevious "내" otherwise "라라"
bottomwhitecups = in.cellNumericValue(2,1)
bottomgreencups = when firstDefinedValue(in.cellNumericValue(2,2),1)<0 0 otherwise firstDefinedValue(in.cellNumericValue(2,2),0)
sbottomgreencups = when bottomgreencups=1 "" otherwise ""
isarebottomgreencups = when bottomgreencups=1 "" otherwise ""
time = b.timeSincePress()
over = this.number("a_{nimComplete}")=1
correct = this.number("I_c")=1
success = when correct "성공입니다!" otherwise ""
incorrectdarker = this.number("g_{reenGuess}")>numericValue("2*${this.number("g_{reenCupsPrevious}")}")
incorrectlighter = this.number("g_{reenGuess}")<numericValue("2*${this.number("g_{reenCupsPrevious}")}")
compare = when correct "원래 페인트 색과 같습니다." when incorrectdarker "원래 페인트 색보다 진합니다." when incorrectlighter "원래 페인트 색보다 연합니다." otherwise ""

```

## Script 3: Screen 2 082c48f5-baf5-4ebc-98ce-0d50318545fa (exhibit/text)

```cl
graph = graphIntro1
usePrevious = when graph.number("w_{hiteCups}") = graph.number("g_{reenCups}") false otherwise true

content: 
when usePrevious 
"여러분이 이전 화면에서 만든 페인트입니다.

라라도 같은 색의 페인트를 만들고 싶어 합니다.

라라가 여러분이 만든 페인트와 같은 페인트를 만들려면 흰색 페인트 ${graph2.number("w_{hiteCups}")}통에 초록색 페인트를 얼마나 섞어야 할까요?"

otherwise 
"라라는 흰색 페인트 2통과 초록색 페인트 3통을 섞어서 새로운 페인트를 만들었습니다.

같은 색의 페인트를 만들려면 흰색 페인트 4통에 초록색 페인트를 얼마나 섞어야 할까요?"
```

## Script 4: Screen 2 inputCheck (table)

```cl
graph = graphIntro1
b = button2
white = graph.number("w_{hiteCups}")
usePrevious = when graph.number("w_{hiteCups}") = graph.number("g_{reenCups}") false otherwise true
cellContent(1, 1):  when usePrevious "${graph.number("w_{hiteCups}")}" otherwise "2"
cellContent(1, 2):  when usePrevious "${graph.number("g_{reenCups}")}" otherwise "3"
cellContent(2, 1):  when usePrevious "${numericValue("2*${white}")}" otherwise "4"

correct: correct

correct = this.cellNumericValue(2,2) = numericValue("2*${this.cellNumericValue(1,2)}")

error = when this.cellNumericValue(2,2) >=0 "" otherwise "양수를 입력하세요."
warning: when isBlank(this.cellContent(2,2)) "" otherwise error 
cellErrorMessage(2,2): error

export("green cup matches"): 
  when b.pressCount = 0 this.cellContent(2,2)
  when b.pressCount = 1 "${b.history("attempts").elementAt(1)}${when isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) = b.history("attempts").elementAt(1) "" otherwise ", ${this.cellContent(2,2)}"}"
  when b.pressCount = 2 "${b.history("attempts").elementAt(1)}, ${b.history("attempts").elementAt(2)}${when isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) = b.history("attempts").elementAt(2) "" otherwise ", ${this.cellContent(2,2)}"}"
  otherwise "${b.history("attempts").elementAt(1)}, ${b.history("attempts").elementAt(2)}, ${this.cellContent(2,2)}"

export("attempts"): "${b.pressCount}"
```

## Script 5: Screen 2 button2 (action-button)

```cl
g = graph2
t = inputCheck

resetLabel: when g.number("a_{nimComplete}") = 1 "재시도" otherwise ""
disabled: this.timeSincePress() > 0 or not(inputCheck.cellNumericValue(2,2) >= 0)
hidden: g.number("a_{nimComplete}") = 1 and inputCheck.script.correct

capture("attempts"): t.cellNumericValue(2,2)


```

## Script 6: Screen 3 f6ced7ec-d46a-419a-86f2-b2fe70653bee (sketch)

```cl
initialColor: colors.black

narration: "그림판에는 흰색 페인트 세 통과 파란색 페인트 네 통으로 만든 페인트가 표시되어 있습니다."
```

## Script 7: Screen 4 a2graph (input/graph)

```cl
t = tablePaint
b = buttonPaint

readOnly: true
trace: false

bounds: makeBounds(this.number("x_m"), this.number("x_M"), this.number("y_m"), this.number("y_M"))

number("t_0"): b.timeSincePress()

number("r_{edGuess}"):   t.cellNumericValue(2, 2)
number("r_{edCups}"):    t.cellNumericValue(1, 2)
number("w_{hiteCups}"):  t.cellNumericValue(1, 1)
number("w_{hiteGuess}"): t.cellNumericValue(2, 1)

narration(1): "책상, 컴퓨터, 램프, 책장 및 창문이 있는 방 그림이 표시되어 있으며, 벽의 일부는 흰색 페인트 네 통과 빨간색 페인트 여섯 통을 섞은 페인트로 칠해져 있습니다."
narration(2): 
  when time "벽의 나머지 부분은 흰색 페인트 한 통과 빨간색 페인트 ${red}통을 섞은 페인트로 칠해져 있습니다."
  otherwise ""
narration(3):
  when over and correct "성공입니다! 페인트 색이 원래 페인트 색과 같습니다."
  when over and incorrect "${compare}"
  otherwise ""
time = b.timeSincePress()>0
red = t.cellNumericValue(2,2)
sred = when red=1 "" otherwise ""
over = this.number("a_{nimComplete}")=1
correct = this.number("I_c")=1
incorrect = this.number("I_c")=0
compare = 
  when numericValue("${t.cellNumericValue(2,2)}-${this.number("r_{ealRed}")}")>0 and this.number("w_{hiteGuessRatio}")<0.4 "원래 페인트 색보다 진합니다."
  when numericValue("${t.cellNumericValue(2,2)}-${this.number("r_{ealRed}")}")>0 "원래 페인트 색보다 진합니다." 
  when numericValue("${t.cellNumericValue(2,2)}-${this.number("r_{ealRed}")}")<0 "원래 페인트 색보다 연합니다."
  otherwise ""



```

## Script 8: Screen 4 tablePaint (table)

```cl
b = buttonPaint
p = patterns
number = p.anyOf(p.number,p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(2,2)) or (number.matches(this.cellContent(2,2))) and this.cellNumericValue(2,2) >= 0)
errorM = "양수를 입력하세요."

#cellErrorMessage(2,2): when this.cellNumericValue(2,2) >= 0 "" otherwise "양수를 입력하세요."

cellErrorMessage(2,2): 
  when error1 errorM 
  otherwise ""

warning: 
  when isBlank(this.cellContent(2,2)) ""
  when error1 errorM 
  otherwise ""

cellDisableEvaluation(2,2): true

correct: this.cellNumericValue(2,2) = 1.5

export("red cup matches"):
  when b.pressCount = 0 this.cellContent(2,2)
  when b.pressCount = 1 "${b.history("attempts").elementAt(1)}${when isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) = b.history("attempts").elementAt(1) "" otherwise ", ${this.cellContent(2,2)}"}"
  when b.pressCount = 2 "${b.history("attempts").elementAt(1)}, ${b.history("attempts").elementAt(2)}${when isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) = b.history("attempts").elementAt(2) "" otherwise ", ${this.cellContent(2,2)}"}"
  otherwise "${b.history("attempts").elementAt(1)}, ${b.history("attempts").elementAt(2)}, ${this.cellContent(2,2)}"
export("attempts"): "${b.pressCount}"
```

## Script 9: Screen 4 buttonPaint (action-button)

```cl
t = tablePaint
g = a2graph

#disabled: this.timeSincePress() > 0 or not(t.cellNumericValue(2,2) >= 0)
disabled: this.timeSincePress() > 0 or t.script.error1 or isBlank(t.cellContent(2,2))

resetLabel: when g.number("a_{nimComplete}") = 1 "재시도" otherwise ""

hidden: g.number("a_{nimComplete}") = 1 and g.number("I_c") = 1

capture("attempts"): t.cellNumericValue(2,2) 
```

## Script 10: Screen 5 graphMixer3 (input/graph)

```cl
b = buttonMixer3
t = tableMixer3

readOnly: true
trace: false

bounds: makeBounds(this.number("x_m"), this.number("x_M"), this.number("y_m"), this.number("y_M"))

number("t_0"): b.timeSincePress() 

number("g_{reenCups}"): 0
number("g_{reenGuess}"): 0
number("b_{lueCups}"):   firstDefinedValue(t.cellNumericValue(1, 1),0)
number("w_{hiteCups}"):  firstDefinedValue(t.cellNumericValue(1, 3),0)
number("r_{edCups}"):    firstDefinedValue(t.cellNumericValue(1, 2),0)
number("b_{lueGuess}"):  firstDefinedValue(t.cellNumericValue(2, 1),0)
number("w_{hiteGuess}"): firstDefinedValue(t.cellNumericValue(2, 3),0)
number("r_{edGuess}"):   firstDefinedValue(t.cellNumericValue(2, 2),0)

narration(1): "세 개의 포스터가 걸린 벽과 소파가 있는 방 그림이 그려져 있으며, 벽의 일부는 파란색 페인트 열두 통, 빨간색 페인트 아홉 통, 흰색 페인트 열네 통을 섞은 페인트로 칠해져 있습니다."
narration(2): 
  when time "벽의 나머지 부분은 파란색 페인트 네 통, 빨간색 페인트 ${red}통, 흰색 페인트 ${white}통을 섞은 페인트로 칠해져 있습니다."
  otherwise ""
narration(3):
  when lessred or redder "${redtext}"
  otherwise ""
narration(4):
  when lessblue or blueer "${bluetext}"
  otherwise ""
narration(5):
  when correct "성공입니다! 원래 페인트 색과 같습니다."
  when lighter or darker "${lightdarktext}"
  otherwise ""
red = firstDefinedValue(t.cellNumericValue(2,2),0)
redactual = this.number("r_{edActual}")
sred = when red=1 "" otherwise ""
white = firstDefinedValue(t.cellNumericValue(2,3),0)
whiteactual = this.number("w_{hiteActual}")
whiteratio = this.number("w_{hiteRatio}")
whiteguessratio = this.number("w_{hiteGuessRatio}")
swhite = when white=1 "" otherwise ""
time = b.timeSincePress()>0
over = this.number("a_{nimComplete}")=1
correct = over and this.number("I_c")=1
incorrect = over and this.number("I_c")=0
tolerance = this.number("t_{olerance}")
ntolerance = numericValue("-1*${tolerance}")
lighter = incorrect and numericValue("${white}-${whiteactual}")>tolerance and (numericValue("1.25*${whiteratio}")<whiteguessratio or red=redactual)
darker = incorrect and numericValue("${white}-${whiteactual}")<ntolerance and (numericValue("0.75*${whiteratio}")>whiteguessratio or red=redactual)
lessred = incorrect and numericValue("${red}-${redactual}")<ntolerance and numericValue("${white}-${whiteactual}")>=0
redder = incorrect and numericValue("${red}-${redactual}")>tolerance and numericValue("${white}-${whiteactual}")<=0
lessblue = incorrect and numericValue("${white}-${whiteactual}")>tolerance and numericValue("${red}-${redactual}")>tolerance
blueer = incorrect and numericValue("${white}-${whiteactual}")<ntolerance and numericValue("${red}-${redactual}")<ntolerance
lightdarktext = when lighter "원래 페인트 색보다 연합니다." otherwise "원래 페인트 색보다 진합니다."
redtext = when lessred "빨간색을 너무 적게 넣었습니다." otherwise "빨간색을 너무 많이 넣었습니다."
bluetext = when lessblue "파란색을 너무 적게 넣었습니다." otherwise "파란색을 너무 많이 넣었습니다."



```

## Script 11: Screen 5 tableMixer3 (table)

```cl
b = buttonMixer3
p = patterns
number = p.anyOf(p.number,p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(2,2)) or (number.matches(this.cellContent(2,2)) and this.cellNumericValue(2,2) > 0))
error2 = not(isBlank(this.cellContent(2,3)) or (number.matches(this.cellContent(2,3)) and this.cellNumericValue(2,3) > 0))
error = error1 or error2
errorM = "양수를 입력하세요."

cellErrorMessage(2,2): when error1 errorM otherwise "" 
cellErrorMessage(2,3): when error2 errorM otherwise "" 
warning: when error errorM otherwise ""

cellDisableEvaluation(2,2): true
cellDisableEvaluation(2,3): true

correct1 = this.cellNumericValue(2,2) = 3
correct2 = this.cellNumericValue(2,3) > 4.66 and this.cellNumericValue(2,3) < 4.67
correct: correct1 and correct2


export("red cup matches"):
  when b.pressCount = 0 this.cellContent(2,2)
  when b.pressCount = 1 "${b.history("attempts").elementAt(1)}${when isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) = b.history("attempts").elementAt(1) "" otherwise ", ${this.cellContent(2,2)}"}"
  when b.pressCount = 2 "${b.history("attempts").elementAt(1)}, ${b.history("attempts").elementAt(2)}${when isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) = b.history("attempts").elementAt(2) "" otherwise ", ${this.cellContent(2,2)}"}"
  otherwise "${b.history("attempts").elementAt(1)}, ${b.history("attempts").elementAt(2)}, ${this.cellContent(2,2)}"
export("red cups correct"): when correct1 "yes" otherwise "no"


export("white cup matches"):
  when b.pressCount = 0 this.cellContent(2,2)
  when b.pressCount = 1 "${b.history("attempts2").elementAt(1)}${when isBlank(this.cellContent(2,3)) or this.cellNumericValue(2,3) = b.history("attempts2").elementAt(1) "" otherwise ", ${this.cellContent(2,3)}"}"
  when b.pressCount = 2 "${b.history("attempts2").elementAt(1)}, ${b.history("attempts2").elementAt(2)}${when isBlank(this.cellContent(2,3)) or this.cellNumericValue(2,3) = b.history("attempts2").elementAt(2) "" otherwise ", ${this.cellContent(2,3)}"}"
  otherwise "${b.history("attempts2").elementAt(1)}, ${b.history("attempts2").elementAt(2)}, ${this.cellContent(2,3)}"
export("white cups correct"): when correct2 "yes" otherwise "no"
export("attempts"): "${b.pressCount}"
```

## Script 12: Screen 5 buttonMixer3 (action-button)

```cl
t = tableMixer3
g = graphMixer3

#disabled: this.timeSincePress() > 0 or not(t.cellNumericValue(2,2) >= 0 and t.cellNumericValue(2,3) >= 0)
disabled: this.timeSincePress() > 0 or t.script.error or isBlank(t.cellContent(2,2)) or isBlank(t.cellContent(2,3))
resetLabel: when g.number("a_{nimComplete}") = 1 "재시도" otherwise ""
hidden: g.number("a_{nimComplete}") = 1 and g.number("I_c") = 1

capture("attempts"): t.cellNumericValue(2,2)
capture("attempts2"): t.cellNumericValue(2,3)
```

## Script 13: Screen 6 77942f06-9973-44c9-8e7c-9b94e15d71c4 (challenge)

```cl
preventChallengeCreationReason: 
  when not(tableMixerCC.script.columns >=2) "페인트 만들기를 완료해 주세요."
  when not(tableMixerCC2.script.valid) "양수를 입력하세요."
  when not(tableMixerCC2.script.unique) "다른 숫자를 입력하세요."
  otherwise ""

preventSubmissionReason: 
  when isBlank(tableMixerCC3.cellContent(2,1)) and isBlank(tableMixerCC3.cellContent(2,2)) and isBlank(tableMixerCC3.cellContent(2,3)) and isBlank(tableMixerCC3.cellContent(2,4)) "양수를 입력해 원래 페인트 색과 같은 페인트를 만드세요."
  when not(tableMixerCC3.script.valid) "양수를 입력하세요."
  when graphMixerCC3.number("a_{nimComplete}") = 1  and graphMixerCC3.number("I_c") = 1 ""
  when graphMixerCC3.number("a_{nimComplete}") = 1 "원래 페인트 색과 같은 페인트를 만드세요."
  otherwise "원래 페인트 색과 같은 페인트를 만드세요."
  
  
challengeBackground: graphLayer(graphMixerCC2.calculatorState)
challengeBounds: makeBounds(0,15,0,15)
#challengeText: 
#"Red: `${when isBlank(tableMixerCC2.cellContent(1,1)) "0" otherwise tableMixerCC2.cellContent(1,1)}`
#Green: `${when isBlank(tableMixerCC2.cellContent(1,2)) "0" otherwise tableMixerCC2.cellContent(1,2)}`
#Blue: `${when isBlank(tableMixerCC2.cellContent(1,3)) "0" otherwise tableMixerCC2.cellContent(1,3)}`
#White: `${when isBlank(tableMixerCC2.cellContent(1,4)) "0" otherwise tableMixerCC2.cellContent(1,4)}`"

#responseText:
#"Red: `${when isBlank(tableMixerCC3.cellContent(2,1)) "0" otherwise tableMixerCC3.cellContent(2,1)}`
#Green: `${when isBlank(tableMixerCC3.cellContent(2,2)) "0" otherwise tableMixerCC3.cellContent(2,2)}`
#Blue: `${when isBlank(tableMixerCC3.cellContent(2,3)) "0" otherwise tableMixerCC3.cellContent(2,3)}`
#White: `${when isBlank(tableMixerCC3.cellContent(2,4)) "0" otherwise tableMixerCC3.cellContent(2,4)}`"
```

## Script 14: Screen 6 graphMixerCC (input/graph)

```cl
table = tableMixerCC
#button = buttonMixerCC
bounds: makeBounds(this.number("x_m"), this.number("x_M"), this.number("y_m"), this.number("y_M"))

number("r_{edCups}"):   when table.cellNumericValue(1, 1) >= 0 table.cellNumericValue(1, 1) otherwise 0
number("b_{lueCups}"):  when table.cellNumericValue(1, 2) >= 0 table.cellNumericValue(1, 2) otherwise 0
number("g_{reenCups}"): when table.cellNumericValue(1, 3) >= 0 table.cellNumericValue(1, 3) otherwise 0
number("w_{hiteCups}"): when table.cellNumericValue(1, 4) >= 0 table.cellNumericValue(1, 4) otherwise 0

trace: false

narration(1): 
  when none "화면에는 빈 페인트통이 표시되어 있습니다."
  when one "화면에는 ${textone} 페인트통이 표시되어 있습니다."
  otherwise "화면에는 새로 만든 페인트통이 표시되어 있습니다."
narration(2):
  when none or one ""
  otherwise "이 페인트는 ${textmore}을 섞어서 만든 페인트입니다."
red = when table.cellNumericValue(1, 1) >= 0 table.cellNumericValue(1, 1) otherwise 0
sred = when red=1 "" otherwise ""
blue = when table.cellNumericValue(1, 2) >= 0 table.cellNumericValue(1, 2) otherwise 0
sblue = when blue=1 "" otherwise ""
green = when table.cellNumericValue(1, 3) >= 0 table.cellNumericValue(1, 3) otherwise 0
sgreen = when green=1 "" otherwise ""
white = when table.cellNumericValue(1, 4) >= 0 table.cellNumericValue(1, 4) otherwise 0
swhite = when white=1 "" otherwise ""
none = red=0 and blue=0 and green=0 and white=0
redone = red>0 and blue=0 and green=0 and white=0
blueone = red=0 and blue>0 and green=0 and white=0
greenone = red=0 and blue=0 and green>0 and white=0
whiteone = red=0 and blue=0 and green=0 and white>0
one = redone or blueone or greenone or whiteone
textone = when redone "빨간색" when blueone "파란색" when greenone "초록색" when whiteone "흰색" otherwise ""
redbluetwo = red>0 and blue>0 and green=0 and white=0
redgreentwo = red>0 and blue=0 and green>0 and white=0
redwhitetwo = red>0 and blue=0 and green=0 and white>0
bluegreentwo = red=0 and blue>0 and green>0 and white=0
bluewhitetwo = red=0 and blue>0 and green=0 and white>0
greenwhitetwo = red=0 and blue=0 and green>0 and white>0
noredthree = red=0 and blue>0 and green>0 and white>0
nobluethree = red>0 and blue=0 and green>0 and white>0
nogreenthree = red>0 and blue>0 and green=0 and white>0
nowhitethree = red>0 and blue>0 and green>0 and white=0
textmore = 
  when redbluetwo "빨간색 페인트 ${red}통과 파란색 페인트 ${blue}통"
  when redgreentwo "빨간색 페인트 ${red}통과 초록색 페인트 ${green}통"
  when redwhitetwo "빨간색 페인트 ${red}통과 흰색 페인트 ${white}통"
  when bluegreentwo "파란색 페인트 ${blue}통과 초록색 페인트 ${green}통"
  when bluewhitetwo "파란색 페인트 ${blue}통과 흰색 페인트 ${white}통"
  when greenwhitetwo "초록색 페인트 ${green}통과 흰색 페인트 ${white}통"
  when noredthree "파란색 페인트 ${blue}통, 초록색 페인트 ${green}통, 흰색 페인트 ${white}통"
  when nobluethree "빨간색 페인트 ${red}통, 초록색 페인트 ${green}통, 흰색 페인트 ${white}통"
  when nogreenthree "빨간색 페인트 ${red}통, 파란색 페인트 ${blue}통, 흰색 페인트 ${white}통"
  when nowhitethree "빨간색 페인트 ${red}통, 파란색 페인트 ${blue}통, 흰색 페인트 ${green}통"
  otherwise "빨간색 페인트 ${red}통, 파란색 페인트 ${blue}통, 초록색 페인트 ${green}통, 흰색 페인트 ${white}통"
```

## Script 15: Screen 6 tableMixerCC (table)

```cl
#hidden: true
#c = choiceCC

#red = when c.isSelected(1) 1 otherwise 0
#blue = 
#  when not(c.isSelected(2)) 0
#  when not(c.isSelected(1)) 1 
#  otherwise 2
#green = 
#  when not(c.isSelected(3)) 0
#  when not(c.isSelected(1)) and not(c.isSelected(2)) 1 
#  when not(c.isSelected(1)) or not(c.isSelected(2)) 2
#  otherwise 3 
#white = 
#  when not(c.isSelected(4)) 0
#  otherwise columns

red = when this.cellNumericValue(1,1) > 0 1 otherwise 0
blue = 
  when not(this.cellNumericValue(1,2) > 0) 0
  when not(this.cellNumericValue(1,1) > 0) 1 
  otherwise 2
green = 
  when not(this.cellNumericValue(1,3) > 0) 0
  when not(this.cellNumericValue(1,1) > 0) and not(this.cellNumericValue(1,2) > 0) 1 
  when not(this.cellNumericValue(1,1) > 0) or not(this.cellNumericValue(1,2) > 0) 2
  otherwise 3 
white = 
  when not(this.cellNumericValue(1,4) > 0) 0
  otherwise columns
 
valid = when red>=0 or white >=0 or green>=0 or blue >=0 1 otherwise 0

  
columns = 
#numericValue("
#  ${when c.isSelected(1) 1 otherwise 0}+
#  ${when c.isSelected(2) 1 otherwise 0}+
#  ${when c.isSelected(3) 1 otherwise 0}+
#  ${when c.isSelected(4) 1 otherwise 0}")
  when not(isBlank(this.cellContent(1,1)) or this.cellNumericValue(1,1) >= 0) or
       not(isBlank(this.cellContent(1,2)) or this.cellNumericValue(1,2) >= 0) or
       not(isBlank(this.cellContent(1,3)) or this.cellNumericValue(1,3) >= 0) or
       not(isBlank(this.cellContent(1,4)) or this.cellNumericValue(1,4) >= 0) 0
  otherwise numericValue("
  ${when not(this.cellNumericValue(1,1) > 0) 0 otherwise 1}+
  ${when not(this.cellNumericValue(1,2) > 0) 0 otherwise 1}+
  ${when not(this.cellNumericValue(1,3) > 0) 0 otherwise 1}+
  ${when not(this.cellNumericValue(1,4) > 0) 0 otherwise 1}")
  
#inputCell1 = 
#  when columns = 2 and red = 1 Table2CC1.cellContent(1,1) 
#  when columns = 3 and red = 1 Table3CC1.cellContent(1,1) 
#  when columns = 4 and red = 1 Table4CC1.cellContent(1,1) otherwise "0"
#inputCell2 = 
#  when columns = 2 and blue = 2 Table2CC1.cellContent(1,2) 
#  when columns = 3 and blue = 2 Table3CC1.cellContent(1,2) 
#  when columns = 4 and blue = 2 Table4CC1.cellContent(1,2)
#  when columns = 2 and blue = 1 Table2CC1.cellContent(1,1) 
#  when columns = 3 and blue = 1 Table3CC1.cellContent(1,1) 
#  when columns = 4 and blue = 1 Table4CC1.cellContent(1,1)  
#  otherwise "0"
#inputCell3 = 
#  when columns = 2 and green = 1 Table2CC1.cellContent(1,1) 
#  when columns = 2 and green = 2 Table2CC1.cellContent(1,2) 
#  when columns = 3 and green = 2 Table3CC1.cellContent(1,2) 
#  when columns = 3 and green = 3 Table3CC1.cellContent(1,3) 
#  when columns = 4 and green = 3 Table4CC1.cellContent(1,3) 
#  otherwise "0"
#inputCell4 = 
#  when columns = 2 and white = 2 Table2CC1.cellContent(1,2)
#  when columns = 3 and white = 3 Table3CC1.cellContent(1,3)
#  when columns = 4 Table4CC1.cellContent(1,4)
#  otherwise "0"
  
#cellContent(1,1): inputCell1
#cellContent(1,2): inputCell2
#cellContent(1,3): inputCell3
#cellContent(1,4): inputCell4

p = patterns
number = p.anyOf(p.number.satisfies("x>=0"),p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(1,1)) or this.cellNumericValue(1,1) >= 0) #number.matches(this.cellContent(1,1)))
error2 = not(isBlank(this.cellContent(1,2)) or this.cellNumericValue(1,2) >= 0) #number.matches(this.cellContent(1,2)))
error3 = not(isBlank(this.cellContent(1,3)) or this.cellNumericValue(1,3) >= 0) #number.matches(this.cellContent(1,3)))
error4 = not(isBlank(this.cellContent(1,4)) or this.cellNumericValue(1,4) >= 0) #number.matches(this.cellContent(1,4)))

error = error1 or error2 or error3 or error4
errorM = "영 또는 양수를 입력하세요."

cellErrorMessage(1,1): when error1 errorM otherwise ""
cellErrorMessage(1,2): when error2 errorM otherwise ""
cellErrorMessage(1,3): when error3 errorM otherwise ""
cellErrorMessage(1,4): when error4 errorM otherwise ""

warning: when error errorM otherwise ""

initialCellContent(1, 1): "0" 
initialCellContent(1, 2): "0" 
initialCellContent(1, 3): "0" 
initialCellContent(1, 4): "0" 



```

## Script 16: Screen 6 graphMixerCC2 (input/graph)

```cl
table = tableMixerCC2
bounds: makeBounds(this.number("x_m"), this.number("x_M"), this.number("y_m"), this.number("y_M"))

number("r_{edCups}"): when colors < 2 0 otherwise firstDefinedValue(table.cellNumericValue(1, 1), 0)
number("b_{lueCups}"): when colors < 2 0 otherwise firstDefinedValue(table.cellNumericValue(1, 2), 0)
number("g_{reenCups}"): when colors < 2 0 otherwise firstDefinedValue(table.cellNumericValue(1, 3), 0)
number("w_{hiteCups}"): when colors < 2 1 otherwise firstDefinedValue(table.cellNumericValue(1, 4), 0)

number("r_{ealIndex}"): graphMixerCC3.script.realIndex
number("s_{etCups}"): graphMixerCC3.script.setCups

colors = numericValue("
  ${when tableMixerCC.cellNumericValue(1,1) > 0 1 otherwise 0}+
  ${when tableMixerCC.cellNumericValue(1,2) > 0 1 otherwise 0}+
  ${when tableMixerCC.cellNumericValue(1,3) > 0 1 otherwise 0}+
  ${when tableMixerCC.cellNumericValue(1,4) > 0 1 otherwise 0}")
  
number("v_{alid}"): tableMixerCC.script.valid 
trace: false

narration(1): "화면에는 책장과 문이 있는 방이 그려져 있습니다."
narration(2): 
  when goback ""
  otherwise "미리보기용 사각형에는 ${graphMixerCC.script.textmore}을 섞어서 만든 페인트가 칠해져 있습니다."
needpositive = tableMixerCC.cellNumericValue(1,1)<0 or tableMixerCC.cellNumericValue(1,2)<0 or tableMixerCC.cellNumericValue(1,3)<0 or tableMixerCC.cellNumericValue(1,4)<0 
goback = needpositive or colors<2

```

## Script 17: Screen 6 10f96d6b-cc02-4fb4-8044-bafe2ed44822 (exhibit/text)

```cl
t = tableMixerCC
content: 
  when t.cellNumericValue(1,1) < 0 or t.cellNumericValue(1,2) < 0 or t.cellNumericValue(1,3) < 0 or t.cellNumericValue(1,4) < 0 "뒤로 돌아가 양수를 입력해 주세요." 
  when t.script.columns < 2 "뒤로 돌아가 2개 이상의 페인트를 선택해 주세요." 
  otherwise "여기에 여러분의 페인트가 표시됩니다.\n\n여러분의 친구가 여러분의 페인트의 색과 같은 페인트를 만들 수 있도록 해 보세요.\n\n숫자를 입력했으면 다음 화면으로 넘어가세요."
  
  
  
  #old content
  #when tableMixerCC.script.columns < 2 "Go back and fill in at least 2 colors." otherwise "Now choose a different amount of one type of paint to be color matched."
```

## Script 18: Screen 6 Table4CC2 (table)

```cl
t = tableMixerCC

cellContent(1, 1):  t.cellContent(1, 1)
cellContent(1, 2):  t.cellContent(1, 2)
cellContent(1, 3):  t.cellContent(1, 3)
cellContent(1, 4):  t.cellContent(1, 4)
hidden: not(t.script.columns = 4)

cellEditable(2,1): isBlank(this.cellContent(2,2)) and isBlank(this.cellContent(2,3)) and isBlank(this.cellContent(2,4)) 
cellEditable(2,2): isBlank(this.cellContent(2,1)) and isBlank(this.cellContent(2,3)) and isBlank(this.cellContent(2,4)) 
cellEditable(2,3): isBlank(this.cellContent(2,2)) and isBlank(this.cellContent(2,1)) and isBlank(this.cellContent(2,4)) 
cellEditable(2,4): isBlank(this.cellContent(2,2)) and isBlank(this.cellContent(2,3)) and isBlank(this.cellContent(2,1)) 

#p = patterns
#number = p.anyOf(p.number.satisfies("x>0"),p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(2,1)) or this.cellNumericValue(2,1) > 0) #number.matches(this.cellContent(2,1)))
error2 = not(isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) > 0) #number.matches(this.cellContent(2,2)))
error3 = not(isBlank(this.cellContent(2,3)) or this.cellNumericValue(2,3) > 0) #number.matches(this.cellContent(2,3)))
error4 = not(isBlank(this.cellContent(2,4)) or this.cellNumericValue(2,4) > 0) #number.matches(this.cellContent(2,4)))

error = error1 or error2 or error3 or error4
errorM = "양수를 입력하세요."

cellErrorMessage(2,1):
  when this.cellNumericValue(1,1) = this.cellNumericValue(2,1) "다른 숫자를 입력하세요."
  when error1 errorM
#  when this.cellNumericValue(2,1) > 0 ""
  otherwise ""
  
cellErrorMessage(2,2):
  when this.cellNumericValue(1,2) = this.cellNumericValue(2,2) "다른 숫자를 입력하세요."
  when error2 errorM
#  when this.cellNumericValue(2,2) > 0 ""
  otherwise ""

cellErrorMessage(2,3):
  when this.cellNumericValue(1,3) = this.cellNumericValue(2,3) "다른 숫자를 입력하세요."
  when error3 errorM
#  when this.cellNumericValue(2,3) > 0 ""
  otherwise ""

cellErrorMessage(2,4):
  when this.cellNumericValue(1,4) = this.cellNumericValue(2,4) "다른 숫자를 입력하세요."
  when error4 errorM
#  when this.cellNumericValue(2,4) > 0 ""
  otherwise ""


```

## Script 19: Screen 6 Table3CC2 (table)

```cl
t = tableMixerCC 

hidden: not(t.script.columns = 3)

cellContent(1,1):
  when t.script.red = 1 t.cellContent(1,1)
  when t.script.blue = 1 t.cellContent(1,2)
  when t.script.green = 1 t.cellContent(1,3)
  otherwise ""
cellContent(1,2):
  when t.script.blue = 2 t.cellContent(1,2)
  when t.script.green = 2 t.cellContent(1,3)
  when t.script.white = 2 t.cellContent(1,4)
  otherwise ""
cellContent(1,3):
  when t.script.green = 3 t.cellContent(1,3)
  when t.script.white = 3 t.cellContent(1,4)
  otherwise ""
  
cellContent(0,1):
  when t.script.red = 1 "빨간색"
  when t.script.blue = 1 "파란색"
  when t.script.green = 1 "초록색"
  otherwise ""

cellContent(0,2):
  when t.script.blue = 2 "파란색"
  when t.script.green = 2 "초록색"
  when t.script.white = 2 "흰색"
  otherwise ""
  
cellContent(0,3):
  when t.script.green = 3 "초록색"
  when t.script.white = 3 "흰색"
  otherwise ""

cellEditable(2,1): isBlank(this.cellContent(2,2)) and isBlank(this.cellContent(2,3))
cellEditable(2,2): isBlank(this.cellContent(2,1)) and isBlank(this.cellContent(2,3))
cellEditable(2,3): isBlank(this.cellContent(2,2)) and isBlank(this.cellContent(2,1))

#p = patterns
#number = p.anyOf(p.number.satisfies("x>0"),p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(2,1)) or this.cellNumericValue(2,1) > 0) #number.matches(this.cellContent(2,1)))
error2 = not(isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) > 0) #number.matches(this.cellContent(2,2)))
error3 = not(isBlank(this.cellContent(2,3)) or this.cellNumericValue(2,3) > 0) #number.matches(this.cellContent(2,3)))

error = error1 or error2 or error3
errorM = "양수를 입력하세요."

cellErrorMessage(2,1):
  when this.cellNumericValue(1,1) = this.cellNumericValue(2,1) "다른 숫자를 입력하세요."
  when error1 errorM
#  when this.cellNumericValue(2,1) > 0 ""
  otherwise ""
  
cellErrorMessage(2,2):
  when this.cellNumericValue(1,2) = this.cellNumericValue(2,2) "다른 숫자를 입력하세요."
  when error2 errorM
#  when this.cellNumericValue(2,2) > 0 ""
  otherwise ""

cellErrorMessage(2,3):
  when this.cellNumericValue(1,3) = this.cellNumericValue(2,3) "다른 숫자를 입력하세요."
  when error3 errorM
#  when this.cellNumericValue(2,3) > 0 ""
  otherwise ""

```

## Script 20: Screen 6 Table2CC2 (table)

```cl
t = tableMixerCC 

hidden: not(t.script.columns = 2)

cellContent(1,1):
  when t.script.red = 1 t.cellContent(1,1)
  when t.script.blue = 1 t.cellContent(1,2)
  when t.script.green = 1 t.cellContent(1,3)
  otherwise ""
cellContent(1,2):
  when t.script.blue = 2 t.cellContent(1,2)
  when t.script.green = 2 t.cellContent(1,3)
  when t.script.white = 2 t.cellContent(1,4)
  otherwise ""
  
cellContent(0,1):
  when t.script.red = 1 "빨간색"
  when t.script.blue = 1 "파란색"
  when t.script.green = 1 "초록색"
  otherwise ""

cellContent(0,2):
  when t.script.blue = 2 "파란색"
  when t.script.green = 2 "초록색"
  when t.script.white = 2 "흰색"
  otherwise ""

cellEditable(2,1): isBlank(this.cellContent(2,2))
cellEditable(2,2): isBlank(this.cellContent(2,1))

#p = patterns
#number = p.anyOf(p.number.satisfies("x>0"),p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(2,1)) or this.cellNumericValue(2,1) > 0) #number.matches(this.cellContent(2,1)))
error2 = not(isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) > 0) #number.matches(this.cellContent(2,2)))

error = error1 or error2
errorM = "양수를 입력하세요."

cellErrorMessage(2,1):
  when this.cellNumericValue(1,1) = this.cellNumericValue(2,1) "다른 숫자를 입력하세요."
  when error1 errorM
#  when this.cellNumericValue(2,1) > 0 ""
  otherwise ""
  
cellErrorMessage(2,2):
  when this.cellNumericValue(1,2) = this.cellNumericValue(2,2) "다른 숫자를 입력하세요."
  when error2 errorM
#  when this.cellNumericValue(2,2) > 0 ""
  otherwise ""

warning: when error1 or error2 errorM otherwise ""
```

## Script 21: Screen 6 tableMixerCC2 (table)

```cl
t = tableMixerCC


cell1 = when t.script.columns = 2 Table2CC2.cellContent(1,1) when t.script.columns = 3 Table3CC2.cellContent(1,1) when t.script.columns = 4 Table4CC2.cellContent(1,1) otherwise ""
cell2 = when t.script.columns = 2 Table2CC2.cellContent(1,2) when t.script.columns = 3 Table3CC2.cellContent(1,2) when t.script.columns = 4 Table4CC2.cellContent(1,2) otherwise ""
cell3 = when t.script.columns = 3 Table3CC2.cellContent(1,3) when t.script.columns = 4 Table4CC2.cellContent(1,4) otherwise ""
cell4 = Table4CC2.cellContent(1,4)
cellContent(1,1): t.cellContent(1,1)
cellContent(1,2): t.cellContent(1,2)
cellContent(1,3): t.cellContent(1,3)
cellContent(1,4): t.cellContent(1,4)


red = t.script.red
blue = t.script.blue
green = t.script.green
white = t.script.white
inputCell1 = 
  when t.script.columns = 2 and red = 1 Table2CC2.cellContent(2,1) 
  when t.script.columns = 3 and red = 1 Table3CC2.cellContent(2,1) 
  when t.script.columns = 4 and red = 1 Table4CC2.cellContent(2,1) otherwise ""
inputCell2 = 
  when t.script.columns = 2 and blue = 2 Table2CC2.cellContent(2,2) 
  when t.script.columns = 3 and blue = 2 Table3CC2.cellContent(2,2) 
  when t.script.columns = 4 and blue = 2 Table4CC2.cellContent(2,2)
  when t.script.columns = 2 and blue = 1 Table2CC2.cellContent(2,1) 
  when t.script.columns = 3 and blue = 1 Table3CC2.cellContent(2,1) 
  when t.script.columns = 4 and blue = 1 Table4CC2.cellContent(2,1)  
  otherwise ""
inputCell3 = 
  when t.script.columns = 2 and green = 1 Table2CC2.cellContent(2,1) 
  when t.script.columns = 2 and green = 2 Table2CC2.cellContent(2,2) 
  when t.script.columns = 3 and green = 2 Table3CC2.cellContent(2,2) 
  when t.script.columns = 3 and green = 3 Table3CC2.cellContent(2,3) 
  when t.script.columns = 4 and green = 3 Table4CC2.cellContent(2,3) 
  otherwise ""
inputCell4 = 
  when t.script.columns = 2 and white = 2 Table2CC2.cellContent(2,2)
  when t.script.columns = 3 and white = 3 Table3CC2.cellContent(2,3)
  when t.script.columns = 4 Table4CC2.cellContent(2,4)
  otherwise ""
  


cellContent(2,1): inputCell1
cellContent(2,2): inputCell2
cellContent(2,3): inputCell3
cellContent(2,4): inputCell4

hidden: true

cellErrorMessage(2,1):
  when this.cellNumericValue(1,1) = this.cellNumericValue(2,1) "다른 숫자를 입력하세요."
  when this.cellNumericValue(2,1) > 0 ""
  otherwise "양수를 입력하세요."
  
cellErrorMessage(2,2):
  when this.cellNumericValue(1,2) = this.cellNumericValue(2,2) "다른 숫자를 입력하세요."
  when this.cellNumericValue(2,2) > 0 ""
  otherwise "양수를 입력하세요."

cellErrorMessage(2,3):
  when this.cellNumericValue(1,3) = this.cellNumericValue(2,3) "다른 숫자를 입력하세요."
  when this.cellNumericValue(2,3) > 0 ""
  otherwise "양수를 입력하세요."

cellErrorMessage(2,4):
  when this.cellNumericValue(1,4) = this.cellNumericValue(2,4) "다른 숫자를 입력하세요."
  when this.cellNumericValue(2,4) > 0 ""
  otherwise "양수를 입력하세요."

unique4 = 
  not(Table4CC2.cellNumericValue(1,1) = Table4CC2.cellNumericValue(2,1)) and
  not(Table4CC2.cellNumericValue(1,2) = Table4CC2.cellNumericValue(2,2)) and
  not(Table4CC2.cellNumericValue(1,3) = Table4CC2.cellNumericValue(2,3)) and
  not(Table4CC2.cellNumericValue(1,4) = Table4CC2.cellNumericValue(2,4))
valid4 = 
  (isBlank(Table4CC2.cellContent(2,1)) or Table4CC2.cellNumericValue(2,1) > 0) and
  (isBlank(Table4CC2.cellContent(2,2)) or Table4CC2.cellNumericValue(2,2) > 0) and
  (isBlank(Table4CC2.cellContent(2,3)) or Table4CC2.cellNumericValue(2,3) > 0) and
  (isBlank(Table4CC2.cellContent(2,4)) or Table4CC2.cellNumericValue(2,4) > 0) and
  not(isBlank(Table4CC2.cellContent(2,1)) and isBlank(Table4CC2.cellContent(2,2)) and isBlank(Table4CC2.cellContent(2,3)) and isBlank(Table4CC2.cellContent(2,4)))
unique3 = 
  not(Table3CC2.cellNumericValue(1,1) = Table3CC2.cellNumericValue(2,1)) and
  not(Table3CC2.cellNumericValue(1,2) = Table3CC2.cellNumericValue(2,2)) and
  not(Table3CC2.cellNumericValue(1,3) = Table3CC2.cellNumericValue(2,3))
valid3 = 
  (isBlank(Table3CC2.cellContent(2,1)) or Table3CC2.cellNumericValue(2,1) > 0) and
  (isBlank(Table3CC2.cellContent(2,2)) or Table3CC2.cellNumericValue(2,2) > 0) and
  (isBlank(Table3CC2.cellContent(2,3)) or Table3CC2.cellNumericValue(2,3) > 0) and
  not(isBlank(Table3CC2.cellContent(2,1)) and isBlank(Table3CC2.cellContent(2,2)) and isBlank(Table3CC2.cellContent(2,3)))
unique2 = 
  not(Table2CC2.cellNumericValue(1,1) = Table2CC2.cellNumericValue(2,1)) and
  not(Table2CC2.cellNumericValue(1,2) = Table2CC2.cellNumericValue(2,2))
valid2 =
  (isBlank(Table2CC2.cellContent(2,1)) or Table2CC2.cellNumericValue(2,1) > 0) and
  (isBlank(Table2CC2.cellContent(2,2)) or Table2CC2.cellNumericValue(2,2) > 0) and
  not(isBlank(Table2CC2.cellContent(2,1)) and isBlank(Table2CC2.cellContent(2,2)))
valid = (t.script.columns = 4 and valid4) or (t.script.columns = 3 and valid3) or (t.script.columns = 2 and valid2)
unique = unique4 and unique3 and unique2
```

## Script 22: Screen 6 graphMixerCC3 (input/graph)

```cl
button = buttonCC3
table = tableMixerCC3
tablePrevious = tableMixerCC2
number("t_0"): button.timeSincePress() 
number("r_{edCups}"): firstDefinedValue(tablePrevious.cellNumericValue(1, 1), 0)
number("b_{lueCups}"): firstDefinedValue(tablePrevious.cellNumericValue(1, 2), 0)
number("g_{reenCups}"): firstDefinedValue(tablePrevious.cellNumericValue(1, 3), 0)
number("w_{hiteCups}"): firstDefinedValue(tablePrevious.cellNumericValue(1, 4), 0)

number("r_{edGuess}"): firstDefinedValue(table.cellNumericValue(2, 1), 0)
number("b_{lueGuess}"): firstDefinedValue(table.cellNumericValue(2, 2), 0)
number("g_{reenGuess}"): firstDefinedValue(table.cellNumericValue(2, 3), 0)
number("w_{hiteGuess}"): firstDefinedValue(table.cellNumericValue(2, 4), 0)

realIndex= when not(isBlank(tablePrevious.cellContent(2, 1))) 1
           when not(isBlank(tablePrevious.cellContent(2, 2))) 2
           when not(isBlank(tablePrevious.cellContent(2, 3))) 3
           when not(isBlank(tablePrevious.cellContent(2, 4))) 4
           otherwise 0
           
setCups= when realIndex = 1 tablePrevious.cellNumericValue(2, 1)
         when realIndex = 2 tablePrevious.cellNumericValue(2, 2)
         when realIndex = 3 tablePrevious.cellNumericValue(2, 3)
         when realIndex = 4 tablePrevious.cellNumericValue(2, 4)
         otherwise 0
         
number("r_{ealIndex}"): realIndex
number("s_{etCups}"): setCups


bounds: makeBounds(this.number("x_m"), this.number("x_M"), this.number("y_m"), this.number("y_M"))

trace: false

narration(1): "책장과 문이 있는 방이 그려져 있으며, 방의 벽에는 포스터가 걸려 있습니다. 방 그림은 이전 화면의 방 그림과 동일하며, 벽 일부가 ${graphMixerCC.script.textmore}을 섞어서 만든 페인트로 칠해져 있습니다."
narration(2): 
  when time "나머지 벽은 ${text234}을 섞어서 만든 페인트로 칠해져 있습니다."
  otherwise ""
narration(3):
  when correct "성공입니다! 페인트의 색이 같습니다."
  when lighter or darker "${lightdarktext}"
  otherwise ""
narration(4):
  when lessblue or blueer "${bluetext}"
  otherwise ""
narration(5):
  when lessgreen or greener "${greentext}"
  otherwise ""
narration(6):
  when lessred or redder "${redtext}"
  otherwise ""
red = firstDefinedValue(table.cellNumericValue(2,1),0)
sred = when red=1 "" otherwise ""
blue = firstDefinedValue(table.cellNumericValue(2,2),0)
sblue = when blue=1 "" otherwise ""
green = firstDefinedValue(table.cellNumericValue(2,3),0)
sgreen = when green=1 "" otherwise ""
white = firstDefinedValue(table.cellNumericValue(2,4),0)
swhite = when white=1 "" otherwise ""
redbluetwo = red>0 and blue>0 and green=0 and white=0
redgreentwo = red>0 and blue=0 and green>0 and white=0
redwhitetwo = red>0 and blue=0 and green=0 and white>0
bluegreentwo = red=0 and blue>0 and green>0 and white=0
bluewhitetwo = red=0 and blue>0 and green=0 and white>0
greenwhitetwo = red=0 and blue=0 and green>0 and white>0
noredthree = red=0 and blue>0 and green>0 and white>0
nobluethree = red>0 and blue=0 and green>0 and white>0
nogreenthree = red>0 and blue>0 and green=0 and white>0
nowhitethree = red>0 and blue>0 and green>0 and white=0
text234 = 
  when redbluetwo "빨간색 페인트 ${red}통과 파란색 페인트 ${blue}통"
  when redgreentwo "빨간색 페인트 ${red}통과 초록색 페인트 ${green}통"
  when redwhitetwo "빨간색 페인트 ${red}통과 흰색 페인트 ${white}통"
  when bluegreentwo "파란색 페인트 ${blue}통과 초록색 페인트 ${green}통"
  when bluewhitetwo "파란색 페인트 ${blue}통과 흰색 페인트 ${white}통"
  when greenwhitetwo "초록색 페인트 ${green}통과 흰색 페인트 ${white}통"
  when noredthree "파란색 페인트 ${blue}통, 초록색 페인트 ${green}통, 흰색 페인트 ${white}통"
  when nobluethree "빨간색 페인트 ${red}통, 초록색 페인트 ${green}통, 흰색 페인트 ${white}통"
  when nogreenthree "빨간색 페인트 ${red}통, 파란색 페인트 ${blue}통, 흰색 페인트 ${white}통"
  when nowhitethree "빨간색 페인트 ${red}통, 파란색 페인트 ${blue}통, 흰색 페인트 ${green}통"
  otherwise "빨간색 페인트 ${red}통, 파란색 페인트 ${blue}통, 초록색 페인트 ${green}통, 흰색 페인트 ${white}통"
time = button.timeSincePress()>0
over = this.number("a_{nimComplete}")=1
correct = over and this.number("I_c")=1
incorrect = over and this.number("I_c")=0
tolerance = this.number("t_{olerance}")
ntolerance = numericValue("-1*${tolerance}")
lessblue = incorrect and this.number("b_{lueCheckLess}")=1
blueer = incorrect and this.number("b_{lueCheckGreater}")=1
bluetext = when lessblue "파란색을 너무 적게 넣었습니다." otherwise "파란색을 너무 많이 넣었습니다."
lessgreen = incorrect and this.number("g_{reenCheckLess}")=1
greener = incorrect and this.number("g_{reenCheckGreater}")=1
greentext = when lessgreen "초록색을 너무 적게 넣었습니다." otherwise "초록색을 너무 많이 넣었습니다."
lessred = incorrect and this.number("r_{edCheckLess}")=1
redder = incorrect and this.number("r_{edCheckGreater}")=1
redtext = when lessred "빨간색을 너무 적게 넣었습니다." otherwise "빨간색을 너무 많이 넣었습니다."
whiteactual = this.number("w_{hiteActual}")
redcheck = this.number("r_{edCheck}")
bluecheck = this.number("b_{lueCheck}")
greencheck = this.number("g_{reenCheck}")
sumrbgchecks0 = numericValue("${redcheck}+${bluecheck}+${greencheck}")=0
whitecups = firstDefinedValue(tablePrevious.cellNumericValue(1,4),0)
totalcups = this.number("t_{otalCups}")
totalguess = this.number("t_{otalGuess}")
wcotc = numericValue("${whitecups}/${totalcups}")
wgotg = numericValue("${white}/${totalguess}")
lighter = incorrect and ( numericValue("${white}-${whiteactual}")>tolerance  or ( realIndex=4 and sumrbgchecks0 ) ) and (wcotc<=wgotg) 
darker =  incorrect and ( numericValue("${white}-${whiteactual}")<ntolerance or ( realIndex=4 and sumrbgchecks0 ) ) and (wcotc>=wgotg)
lightdarktext = when lighter "원래 페인트 색보다 연합니다." otherwise "원래 페인트 색보다 진합니다."

```

## Script 23: Screen 6 Table4CC3 (table)

```cl
hidden: not(tableMixerCC.script.columns = 4)

cellContent(1,1): Table4CC2.cellContent(1,1)
cellContent(1,2): Table4CC2.cellContent(1,2)
cellContent(1,3): Table4CC2.cellContent(1,3)
cellContent(1,4): Table4CC2.cellContent(1,4)
cellContent(2,1): Table4CC2.cellContent(2,1)
cellContent(2,2): Table4CC2.cellContent(2,2)
cellContent(2,3): Table4CC2.cellContent(2,3)
cellContent(2,4): Table4CC2.cellContent(2,4)

cellEditable(2,1): isBlank(Table4CC2.cellContent(2,1))
cellEditable(2,2): isBlank(Table4CC2.cellContent(2,2))
cellEditable(2,3): isBlank(Table4CC2.cellContent(2,3))
cellEditable(2,4): isBlank(Table4CC2.cellContent(2,4))

#cellErrorMessage(2,1): when this.cellNumericValue(2,1) >= 0 "" otherwise "양수를 입력하세요."
#cellErrorMessage(2,2): when this.cellNumericValue(2,2) >= 0 "" otherwise "양수를 입력하세요."
#cellErrorMessage(2,3): when this.cellNumericValue(2,3) >= 0 "" otherwise "양수를 입력하세요."
#cellErrorMessage(2,4): when this.cellNumericValue(2,4) >= 0 "" otherwise "양수를 입력하세요."

#p = patterns
#number = p.anyOf(p.number.satisfies("x>=0"),p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(2,1)) or this.cellNumericValue(2,1) > 0) #number.matches(this.cellContent(2,1)))
error2 = not(isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) > 0) #number.matches(this.cellContent(2,2)))
error3 = not(isBlank(this.cellContent(2,3)) or this.cellNumericValue(2,3) > 0) #number.matches(this.cellContent(2,3)))
error4 = not(isBlank(this.cellContent(2,4)) or this.cellNumericValue(2,4) > 0) #number.matches(this.cellContent(2,4)))

error = error1 or error2 or error3 or error4
errorM = "양수를 입력하세요."

cellErrorMessage(2,1): when error1 errorM  otherwise ""
cellErrorMessage(2,2): when error2 errorM  otherwise ""
cellErrorMessage(2,3): when error3 errorM  otherwise ""
cellErrorMessage(2,4): when error4 errorM  otherwise ""

warning: when error errorM otherwise ""
```

## Script 24: Screen 6 Table3CC3 (table)

```cl
hidden: not(tableMixerCC.script.columns = 3)

cellContent(1,1): Table3CC2.cellContent(1,1)
cellContent(1,2): Table3CC2.cellContent(1,2)
cellContent(1,3): Table3CC2.cellContent(1,3)
cellContent(2,1): Table3CC2.cellContent(2,1)
cellContent(2,2): Table3CC2.cellContent(2,2)
cellContent(2,3): Table3CC2.cellContent(2,3)
cellContent(0,1): Table3CC2.cellContent(0,1)
cellContent(0,2): Table3CC2.cellContent(0,2)
cellContent(0,3): Table3CC2.cellContent(0,3)

cellEditable(2,1): isBlank(Table3CC2.cellContent(2,1))
cellEditable(2,2): isBlank(Table3CC2.cellContent(2,2))
cellEditable(2,3): isBlank(Table3CC2.cellContent(2,3))

#cellErrorMessage(2,1): when this.cellNumericValue(2,1) >= 0 "" otherwise "양수를 입력하세요."
#cellErrorMessage(2,2): when this.cellNumericValue(2,2) >= 0 "" otherwise "양수를 입력하세요."
#cellErrorMessage(2,3): when this.cellNumericValue(2,3) >= 0 "" otherwise "양수를 입력하세요."

#p = patterns
#number = p.anyOf(p.number.satisfies("x>=0"),p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(2,1)) or this.cellNumericValue(2,1) > 0) #number.matches(this.cellContent(2,1)))
error2 = not(isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) > 0) #number.matches(this.cellContent(2,2)))
error3 = not(isBlank(this.cellContent(2,3)) or this.cellNumericValue(2,3) > 0) #number.matches(this.cellContent(2,3)))

error = error1 or error2 or error3
errorM = "양수를 입력하세요."

cellErrorMessage(2,1): when error1 errorM  otherwise ""
cellErrorMessage(2,2): when error2 errorM  otherwise ""
cellErrorMessage(2,3): when error3 errorM  otherwise ""

warning: when error errorM otherwise ""
```

## Script 25: Screen 6 Table2CC3 (table)

```cl
hidden: not(tableMixerCC.script.columns = 2)

cellContent(1,1): Table2CC2.cellContent(1,1)
cellContent(1,2): Table2CC2.cellContent(1,2)
cellContent(2,1): Table2CC2.cellContent(2,1)
cellContent(2,2): Table2CC2.cellContent(2,2)
cellContent(0,1): Table2CC2.cellContent(0,1)
cellContent(0,2): Table2CC2.cellContent(0,2)

cellEditable(2,1): isBlank(Table2CC2.cellContent(2,1))
cellEditable(2,2): isBlank(Table2CC2.cellContent(2,2))

#cellErrorMessage(2,1): when this.cellNumericValue(2,1) >= 0 "" otherwise "양수를 입력하세요."
#cellErrorMessage(2,2): when this.cellNumericValue(2,2) >= 0 "" otherwise "양수를 입력하세요."

#p = patterns
#number = p.anyOf(p.number.satisfies("x>=0"),p.fraction(p.integer.satisfies("x>0"),p.integer.satisfies("x>0")),p.radical(p.integer.satisfies("x>0")))

error1 = not(isBlank(this.cellContent(2,1)) or this.cellNumericValue(2,1) > 0) #number.matches(this.cellContent(2,1)))
error2 = not(isBlank(this.cellContent(2,2)) or this.cellNumericValue(2,2) > 0) #number.matches(this.cellContent(2,2)))

error = error1 or error2
errorM = "양수를 입력하세요."

cellErrorMessage(2,1): when error1 errorM  otherwise ""
cellErrorMessage(2,2): when error2 errorM  otherwise ""

warning: when error errorM otherwise ""
```

## Script 26: Screen 6 buttonCC3 (action-button)

```cl
disabled: this.timeSincePress() > 0 or not(tableMixerCC3.script.valid)
resetLabel: when graphMixerCC3.number("a_{nimComplete}") = 1 "초기화" otherwise ""
hidden: graphMixerCC3.number("I_c") = 1 and graphMixerCC3.number("a_{nimComplete}") = 1
```

## Script 27: Screen 6 tableMixerCC3 (table)

```cl
t = tableMixerCC
cellContent(1,1): t.cellContent(1,1)
cellContent(1,2): t.cellContent(1,2)
cellContent(1,3): t.cellContent(1,3)
cellContent(1,4): t.cellContent(1,4)

red = t.script.red
blue = t.script.blue
green = t.script.green
white = t.script.white
inputCell1 = 
  when t.script.columns = 2 and red = 1 Table2CC3.cellContent(2,1) 
  when t.script.columns = 3 and red = 1 Table3CC3.cellContent(2,1) 
  when t.script.columns = 4 and red = 1 Table4CC3.cellContent(2,1) otherwise ""
inputCell2 = 
  when t.script.columns = 2 and blue = 2 Table2CC3.cellContent(2,2) 
  when t.script.columns = 3 and blue = 2 Table3CC3.cellContent(2,2) 
  when t.script.columns = 4 and blue = 2 Table4CC3.cellContent(2,2)
  when t.script.columns = 2 and blue = 1 Table2CC3.cellContent(2,1) 
  when t.script.columns = 3 and blue = 1 Table3CC3.cellContent(2,1) 
  when t.script.columns = 4 and blue = 1 Table4CC3.cellContent(2,1)  
  otherwise ""
inputCell3 = 
  when t.script.columns = 2 and green = 1 Table2CC3.cellContent(2,1) 
  when t.script.columns = 2 and green = 2 Table2CC3.cellContent(2,2) 
  when t.script.columns = 3 and green = 2 Table3CC3.cellContent(2,2) 
  when t.script.columns = 3 and green = 3 Table3CC3.cellContent(2,3) 
  when t.script.columns = 4 and green = 3 Table4CC3.cellContent(2,3) 
  otherwise ""
inputCell4 = 
  when t.script.columns = 2 and white = 2 Table2CC3.cellContent(2,2)
  when t.script.columns = 3 and white = 3 Table3CC3.cellContent(2,3)
  when t.script.columns = 4 Table4CC3.cellContent(2,4)
  otherwise ""

cellContent(2,1): inputCell1
cellContent(2,2): inputCell2
cellContent(2,3): inputCell3
cellContent(2,4): inputCell4

hidden: true


cellErrorMessage(2,1): when this.cellNumericValue(2,1) > 0 "" otherwise "양수를 입력하세요."
cellErrorMessage(2,2): when this.cellNumericValue(2,2) > 0 "" otherwise "양수를 입력하세요."
cellErrorMessage(2,3): when this.cellNumericValue(2,3) > 0 "" otherwise "양수를 입력하세요."
cellErrorMessage(2,4): when this.cellNumericValue(2,4) > 0 "" otherwise "양수를 입력하세요."


valid4 = 
  (isBlank(Table4CC3.cellContent(2,1)) or Table4CC3.cellNumericValue(2,1) >= 0) and
  (isBlank(Table4CC3.cellContent(2,2)) or Table4CC3.cellNumericValue(2,2) >= 0) and
  (isBlank(Table4CC3.cellContent(2,3)) or Table4CC3.cellNumericValue(2,3) >= 0) and
  (isBlank(Table4CC3.cellContent(2,4)) or Table4CC3.cellNumericValue(2,4) >= 0) and
  not(isBlank(Table4CC3.cellContent(2,1)) or isBlank(Table4CC3.cellContent(2,2)) or isBlank(Table4CC3.cellContent(2,3)) or isBlank(Table4CC3.cellContent(2,4)))

valid3 = 
  (isBlank(Table3CC3.cellContent(2,1)) or Table3CC3.cellNumericValue(2,1) >= 0) and
  (isBlank(Table3CC3.cellContent(2,2)) or Table3CC3.cellNumericValue(2,2) >= 0) and
  (isBlank(Table3CC3.cellContent(2,3)) or Table3CC3.cellNumericValue(2,3) >= 0) and
  not(isBlank(Table3CC3.cellContent(2,1)) or isBlank(Table3CC3.cellContent(2,2)) or isBlank(Table3CC3.cellContent(2,3)))

valid2 =
  (isBlank(Table2CC3.cellContent(2,1)) or Table2CC3.cellNumericValue(2,1) >= 0) and
  (isBlank(Table2CC3.cellContent(2,2)) or Table2CC3.cellNumericValue(2,2) >= 0) and
  not(isBlank(Table2CC3.cellContent(2,1)) or isBlank(Table2CC3.cellContent(2,2)))
valid = (t.script.columns = 4 and valid4) or (t.script.columns = 3 and valid3) or (t.script.columns = 2 and valid2)


```

