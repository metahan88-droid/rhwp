# Amplify Activity Inspection

Source URL: https://classroom.amplify.com/activity/5f5802d5032677386597f5da?lang=ko

## Static Metadata

- Activity ID: 5f5802d5032677386597f5da
- Static title: 지수 방정식 찾기
- Description: 학생은 이 액티비티에서 지수 방정식을 작성하는 연습을 통해 지수 함수에 대해 배울 수 있습니다.

한국어 번역: Desmos 현지화 팀
- Canonical: https://classroom.amplify.com/activity/5f5802d5032677386597f5da
- Image: https://uploads.desmos.com/activitybuilder/8dec38c52c678843fdf33e814e198b36

## Activity JSON Summary

- Title: 지수 방정식 찾기
- Version: 1
- Commit ID: 62ff4791ffa995cf2de506c4
- Published: 2022-08-19T08:26:41.061Z
- Edited: 2022-08-19T08:26:41.061Z
- Permission to share: true
- Noindex: false
- Screen count: 8

## Component Counts

- input/graph: 9
- exhibit/text: 9
- input/expression: 3
- table: 2
- action-button: 2
- multiple-choice: 2
- ddf3b744-414c-42b7-86e7-de9b1dafdfde: 2
- 6e36de2a-abf4-4840-b989-95b4b86e75e9: 2
- 3a483182-58cf-4d55-9f7f-54bdeb68f100: 1
- b9d68f93-c2b4-4e3a-bb0e-b3628e69c779: 1
- b4238401-4689-41af-8df0-78e7c29c4c5d: 1
- 123eac8f-7e42-4f43-b397-9ba8d2033f6b: 1
- challenge: 1

## Screens

| # | Title | Type | Components |
|---:|---|---|---|
| 1 | 환영합니다! | two-column | exhibit/text:1, input/graph:1 |
| 2 | 1번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 3 | 2번째 챌린지 | two-column | action-button:1, exhibit/text:1, input/graph:1, table:1 |
| 4 | 논쟁 해결하기 | two-column | 3a483182-58cf-4d55-9f7f-54bdeb68f100:1, 6e36de2a-abf4-4840-b989-95b4b86e75e9:1, b9d68f93-c2b4-4e3a-bb0e-b3628e69c779:1, ddf3b744-414c-42b7-86e7-de9b1dafdfde:1, exhibit/text:1, input/graph:1, multiple-choice:1 |
| 5 | 3번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 6 | 4번째 챌린지 | two-column | action-button:1, exhibit/text:1, input/graph:1, table:1 |
| 7 | 지수 방정식 찾기 | two-column | 123eac8f-7e42-4f43-b397-9ba8d2033f6b:1, 6e36de2a-abf4-4840-b989-95b4b86e75e9:1, b4238401-4689-41af-8df0-78e7c29c4c5d:1, ddf3b744-414c-42b7-86e7-de9b1dafdfde:1, exhibit/text:1, input/graph:1, multiple-choice:1 |
| 8 | (untitled) | two-column | challenge:1, exhibit/text:2, input/expression:1, input/graph:2 |

## CL Scripts

## Script 1: Screen 1 cfe84774-b799-4f40-ad41-db1cfd16a6ce (input/graph)

```cl
readOnly: true

#Narration

narration(1): "그래프에는 X축과 Y축의 범위가 `-2`부터 `6`까지인 좌표평면이 표시되어 있습니다. 또한 2개의 점 `(0,1)`과 `(4,3)`을 지나는 곡선도 표시되어 있습니다."
```

## Script 2: Screen 2 c1graph (input/graph)

```cl
#학생이 수식 입력창에 입력한 값에 따라 함수를 정의합니다.

function("f"): simpleFunction(c1input.latex)

readOnly: true

#Narration

narration(1): "그래프에는 점 `(0,1)`과 점 `(1,3)이 표시되어 있습니다`. 또한 방정식을 입력해 편집할 수 있는 곡선도 표시되어 있습니다."

success = c1graph.number("I_c") = 1

narration(2): when success "잘 하셨어요! 곡선 `${c1input.latex}`이 주어진 점을 지납니다." otherwise ""
```

## Script 3: Screen 2 fd52b546-4f85-4266-8725-3e8c1fa29059 (exhibit/text)

```cl
showWarning = not(c1graph.number("W") = 0)

message = "지수 방정식을 수정해 보세요. 지수함수는 주어진 두 개의 점을 지나야 합니다."
warning = when showWarning "\n\n`y=b^x`와 같이 지수 방정식의 형태로 입력해야 합니다." otherwise ""

content: "${message}${warning}"
```

## Script 4: Screen 2 c1input (input/expression)

```cl
initialLatex:"y=2^{x}"

correct: c1graph.number("I_c") = 1
```

## Script 5: Screen 3 c2graph (input/graph)

```cl
#학생이 표에 입력한 값에 따라 네 개의 함수가 정의됩니다.

function("f_1"): simpleFunction(c2table.cellContent(1,2))
function("f_2"): simpleFunction(c2table.cellContent(2,2))
function("f_3"): simpleFunction(c2table.cellContent(3,2))
function("f_4"): simpleFunction(c2table.cellContent(4,2))

readOnly: true

#애니메이션을 실행합니다.

number("t_p"): c2button.timeSincePress

#표에 정의된 함수의 개수를 세기 위해 계산기에 값을 입력합니다.

number("C_2"): when isDefined(simpleFunction(c2table.cellContent(2,2))) 1 otherwise 0
number("C_3"): when isDefined(simpleFunction(c2table.cellContent(3,2))) 1 otherwise 0
number("C_4"): when isDefined(simpleFunction(c2table.cellContent(4,2))) 1 otherwise 0

#내레이션

narration(1): "좌표평면에는 파란색 점 네 개가 표시되어 있습니다. 파란색 점의 위치는 각각 `(1,6)`, `(1,3.5)`, `(1,2)`, `(1,0.25)`입니다. 또한 `(0,1)`에 검은색 점이 표시되어 있으며, 표에 입력한 지수 방정식의 그래프가 표시되어 있습니다."

success = c2graph.number("I_c") = 1

narration(2): when success "잘 하셨어요! 각각의 지수 함수가 `(0,1)`에 있는 검은 점과 다른 파란색 점 중 하나를 지납니다." otherwise ""
```

## Script 6: Screen 3 c2table (table)

```cl
correct: c2graph.number("I_c") = 1 and c2graph.number("W") = 0

showFeedback = c2graph.number("F") = 1

correct2 = c2graph.number("I_2") = 1
correct3 = c2graph.number("I_3") = 1
correct4 = c2graph.number("I_4") = 1

warning2 = when c2graph.number("W_2") = 0 false otherwise true
warning3 = when c2graph.number("W_3") = 0 false otherwise true
warning4 = when c2graph.number("W_4") = 0 false otherwise true

duplicate2 = when c2graph.number("D_2") = 0 false otherwise true
duplicate3 = when c2graph.number("D_3") = 0 false otherwise true
duplicate4 = when c2graph.number("D_4") = 0 false otherwise true

cellContent(1,3):
  when showFeedback "✅"
  otherwise ""

cellContent(2,3):
  when showFeedback and correct2 and not(warning2) and not(duplicate2) "✅"
  when showFeedback and correct2 and not(warning2) "중복된 방정식"
  when showFeedback and not(correct2) and not(warning2) "❌"
  when showFeedback "지수함수 아님"
  otherwise ""

cellContent(3,3):
  when showFeedback and correct3 and not(warning3) and not(duplicate3) "✅"
  when showFeedback and correct3 and not(warning3) "중복된 방정식"
  when showFeedback and not(correct3) and not(warning3) "❌"
  when showFeedback "지수함수 아님"
  otherwise ""

cellContent(4,3):
  when showFeedback and correct4 and not(warning4) and not(duplicate4) "✅"
  when showFeedback and correct4 and not(warning4) "중복된 방정식"
  when showFeedback and not(correct4) and not(warning4) "❌"
  when showFeedback "지수함수 아님"
  otherwise ""





```

## Script 7: Screen 3 c2button (action-button)

```cl
resetLabel: "초기화"
```

## Script 8: Screen 4 29ae86a4-d8bb-416f-a08a-06d974fb30e7 (input/graph)

```cl
readOnly: true

#Narration

narration(1): "그래프에는 실선으로 표시된 지수함수와 점선으로 표시된 지수함수가 있습니다. 실선으로 표시된 지수함수는 점 `(0,1)`과 `(1,3)`을 지나며, 점선으로 표시된 지수함수는 점 `(0,4)`과 `(1,8)`을 지납니다."
```

## Script 9: Screen 5 c3graph (input/graph)

```cl
#학생이 표에 입력한 값에 따라 네 개의 함수가 정의됩니다.

function("f"): simpleFunction(c3input.latex)

readOnly: true

#Narration

narration(1): "그래프에는 두 개의 점 `(0,1)`과 `(1,12)`가 표시되어 있습니다. 또한 입력창에서 수정할 수 있는 지수함수도 표시되어 있습니다."

success = c3graph.number("I_c") = 1

narration(2): when success "잘 하셨어요! 곡선 `${c3input.latex}`이 주어진 점을 지납니다." otherwise ""
```

## Script 10: Screen 5 52f90786-a309-4103-a44f-a79bddd8147a (exhibit/text)

```cl
showWarning = not(c3graph.number("W") = 0)

message = "지수 방정식을 수정해 보세요. 지수함수가 주어진 두 개의 점을 지나야 합니다."
warning = when showWarning "\n\n`y=a\\cdot b^x`와 같은 지수 방정식의 형태로 입력해야 합니다." otherwise ""

content: "${message}${warning}"
```

## Script 11: Screen 5 c3input (input/expression)

```cl
initialLatex:"y=1\\cdot 3^{x}"

correct: c3graph.number("I_c") = 1 and not(c3graph.number("W") = 1)
errorMessage: when not(c3graph.number("W") = 0) "지수 방정식을 입력해 주세요." otherwise ""
```

## Script 12: Screen 6 c4graph (input/graph)

```cl
#학생이 표에 입력한 값에 따라 네 개의 함수가 정의됩니다.

function("f_1"): simpleFunction(c4table.cellContent(1,2))
function("f_2"): simpleFunction(c4table.cellContent(2,2))
function("f_3"): simpleFunction(c4table.cellContent(3,2))
function("f_4"): simpleFunction(c4table.cellContent(4,2))

readOnly: true

#애니메이션을 실행합니다.

number("t_p"): c4button.timeSincePress

#표에 정의된 함수의 개수를 세기 위해 계산기에 값을 입력합니다.

number("C_2"): when isDefined(simpleFunction(c4table.cellContent(2,2))) 1 otherwise 0
number("C_3"): when isDefined(simpleFunction(c4table.cellContent(3,2))) 1 otherwise 0
number("C_4"): when isDefined(simpleFunction(c4table.cellContent(4,2))) 1 otherwise 0

#내레이션

narration(1): "좌표평면에는 네 개의 파란색 점이 표시되어 있으며, 파란색 점의 위치는 각각 `(0,1)`, `(0,2)`, `(0,6)`, `(0,16)`입니다. 또한 `(1,8)`에 검은색 점이 표시되어 있으며, 표에 입력한 방정식의 그래프가 표시됩니다."

success = c4graph.number("I_c") = 1

narration(2): when success "잘 하셨어요! 각각의 곡선이 `(1,8)`의 검은색 점과 4개의 파란색 점 중 하나를 지납니다." otherwise ""
```

## Script 13: Screen 6 c4table (table)

```cl
correct: c4graph.number("I_c") = 1 and c4graph.number("W") = 0

showFeedback = c4graph.number("F") = 1

correct2 = c4graph.number("I_2") = 1
correct3 = c4graph.number("I_3") = 1
correct4 = c4graph.number("I_4") = 1

warning2 = when c4graph.number("W_2") = 0 false otherwise true
warning3 = when c4graph.number("W_3") = 0 false otherwise true
warning4 = when c4graph.number("W_4") = 0 false otherwise true

duplicate2 = when c4graph.number("D_2") = 0 false otherwise true
duplicate3 = when c4graph.number("D_3") = 0 false otherwise true
duplicate4 = when c4graph.number("D_4") = 0 false otherwise true

cellContent(1,3):
  when showFeedback "✅"
  otherwise ""

cellContent(2,3):
  when showFeedback and correct2 and not(warning2) and not(duplicate2) "✅"
  when showFeedback and correct2 and not(warning2) "중복된 방정식"
  when showFeedback and not(correct2) and not(warning2) "❌"
  when showFeedback "지수함수 아님"
  otherwise ""

cellContent(3,3):
  when showFeedback and correct3 and not(warning3) and not(duplicate3) "✅"
  when showFeedback and correct3 and not(warning3) "중복된 방정식"
  when showFeedback and not(correct3) and not(warning3) "❌"
  when showFeedback "지수함수 아님"
  otherwise ""

cellContent(4,3):
  when showFeedback and correct4 and not(warning4) and not(duplicate4) "✅"
  when showFeedback and correct4 and not(warning4) "중복된 방정식"
  when showFeedback and not(correct4) and not(warning4) "❌"
  when showFeedback "지수함수 아님"
  otherwise ""
```

## Script 14: Screen 6 c4button (action-button)

```cl
resetLabel: "초기화"
```

## Script 15: Screen 7 656137db-b3b2-4967-8b3b-35fd733584a4 (input/graph)

```cl
readOnly: true

#Narration

narration(1): "좌표평면에는 여섯 개의 점을 지나는 지수 함수가 표시되어 있습니다. 여섯 개의 점의 위치는 각각 `(-3,0.375)`, `(-2,0.75)`, `(-1,1.5)`, `(0,3)`, `(1,6)`, `(2,12)`입니다."
```

## Script 16: Screen 8 636fd7aa-afff-4793-8627-62bc0b8667e5 (challenge)

```cl
#학생이 최소 하나의 점을 움직였나요?

pointsMoved = graph9.number("I_1") = 1

#학생이 만든 점이 서로 겹치지는 않나요?

pointsNotStacked = (graph9.number("I_2") = 1)

challengeSolved = (graph10.number("I_c") = 1 and graph10.number("W") = 0 and input10.timeSinceSubmit >= 2)

preventChallengeCreationReason:
  when not(pointsMoved) "최소 하나의 점을 움직여야 합니다."
  when not(pointsNotStacked) "유효한 챌린지를 생성해야 합니다. (힌트: 점을 겹쳐서는 안 됩니다.)"
  otherwise ""

preventSubmissionReason:
  when not(challengeSolved) "점을 이용해 지수 함수를 그려야 합니다."
  otherwise ""

challengeBackground: graphLayer(graph9.calculatorState)
challengeBounds: graph9.bounds

responseText: "`${input10.latex}`"
```

## Script 17: Screen 8 graph9 (input/graph)

```cl
#내레이션

narration(1): "좌표평면의 X축 범위는 `-4`부터 `4`까지이며 Y축 범위는 `-8`부터 `24`까지입니다. 그래프에는 움직일 수 있는 2개의 점이 있으며, 각각의 위치는 `(0,1)`과 `(1,1)`입니다."
```

## Script 18: Screen 8 graph10 (input/graph)

```cl
#그래프 9의 움직일 수 없는 점에 기반해 그래프 10의 고정된 점을 정의합니다.

number("b_1"): graph9.number("b_1")
number("a_2"): graph9.number("a_2")
number("b_2"): graph9.number("b_2")

#애니메이션을 실행합니다.

number("t_p"): input10.timeSinceSubmit

#학생이 수식 입력창에 입력한 값에 따라 함수가 정의됩니다.

function("f"): simpleFunction(input10.latex,"x")

#다음은 두 점의 좌표입니다. 내레이션이 읽을 수 있도록 좌표를 정의해 주세요.

x1 = 0
y1 = graph9.number("b_1")

x2 = graph9.number("a_2")
y2 = graph9.number("b_2")

#내레이션

narration(1): "좌표평면의 X축 범위는 `-4`부터 `4`까지이며 Y축 범위는 `-8`부터 `24`까지입니다. 그래프에는 움직일 수 있는 두 개의 점이 있으며, 각각의 위치는 `(${x1},${y1})'과 `(${x2},${y2})`입니다."

success = graph10.number("I_c") = 1

narration(2): when success "잘 하셨어요! 곡선 `${input10.latex}`이 주어진 점들을 지납니다." otherwise ""
```

