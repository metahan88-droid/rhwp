# Amplify Activity Inspection

Source URL: https://classroom.amplify.com/activity/5fadcf4bae6c6c39d84913e5?lang=ko

## Static Metadata

- Activity ID: 5fadcf4bae6c6c39d84913e5
- Static title: 스키 활강 경기: 직선
- Description: 학생은 이 액티비티에서 스키 활강 경기를 테마로 한 챌린지를 통해 직선의 방정식 및 그래프에 대한 이해도를 높일 수 있습니다.

이 액티비티는 여러 번의 수업에 걸쳐 진행해야 할 수도 있습니다. 액티비티를 나중에 이어서 진행하려면 학생이 로그인하도록 지도해 주세요.

도움 주신 분: Paul Jorgens

한국어 번역: Desmos 현지화 팀
- Canonical: https://classroom.amplify.com/activity/5fadcf4bae6c6c39d84913e5
- Image: https://uploads.desmos.com/activitybuilder/52b6649575336c746b375d0c244d2266

## Activity JSON Summary

- Title: 스키 활강 경기: 직선
- Version: 1
- Commit ID: 630348bd0da1dff397b74575
- Published: 2022-08-22T09:22:31.685Z
- Edited: 2022-08-22T09:22:31.685Z
- Permission to share: true
- Noindex: false
- Screen count: 9

## Component Counts

- exhibit/text: 10
- input/graph: 8
- input/expression: 4
- multiple-choice: 4
- sketch: 2
- background: 2
- 17af4eb3-e206-4dc7-973e-e4555ebcd9fa: 2
- 85b749d0-447b-422b-a11a-e7e7e5fca8fe: 2
- 0a9c2a0b-6d6f-4085-99df-4ab140ba565b: 2
- 97597a3d-4116-45c9-8c5e-3063d6d6140d: 1
- 1d6563ae-8935-4d04-b2f4-f9a387bee90f: 1
- eb86ea45-d77f-4391-a43f-e1ecfbf042bf: 1
- 9f2799da-487b-4b4e-8fb0-328ef96d12e6: 1
- b2d9235a-5b06-400e-892b-a711bd672e83: 1
- challenge: 1
- 633c280c-f0fb-49a6-bdab-4aff292ecadf: 1
- 089a4959-7f91-4886-8a15-52d541bf4505: 1
- 94d737a0-21b6-490e-a7aa-abc252d5c26e: 1

## Screens

| # | Title | Type | Components |
|---:|---|---|---|
| 1 | 준비 활동 1 | two-column | exhibit/text:1, input/graph:1 |
| 2 | 준비 활동 2 | two-column | exhibit/text:1, input/graph:1 |
| 3 | 챌린지 1 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 4 | 챌린지 2 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 5 | 직선의 방정식 찾기 | two-column | 0a9c2a0b-6d6f-4085-99df-4ab140ba565b:1, 17af4eb3-e206-4dc7-973e-e4555ebcd9fa:1, 85b749d0-447b-422b-a11a-e7e7e5fca8fe:1, 97597a3d-4116-45c9-8c5e-3063d6d6140d:1, background:1, exhibit/text:1, multiple-choice:1, sketch:1 |
| 6 | 챌린지 3 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 7 | 논쟁 해결하기 | two-column | 0a9c2a0b-6d6f-4085-99df-4ab140ba565b:1, 17af4eb3-e206-4dc7-973e-e4555ebcd9fa:1, 1d6563ae-8935-4d04-b2f4-f9a387bee90f:1, 85b749d0-447b-422b-a11a-e7e7e5fca8fe:1, background:1, exhibit/text:1, multiple-choice:1, sketch:1 |
| 8 | 해결 불가능한 문제 | two-column | 9f2799da-487b-4b4e-8fb0-328ef96d12e6:1, b2d9235a-5b06-400e-892b-a711bd672e83:1, eb86ea45-d77f-4391-a43f-e1ecfbf042bf:1, exhibit/text:1, input/graph:1, multiple-choice:1 |
| 9 | (untitled) | two-column | 089a4959-7f91-4886-8a15-52d541bf4505:1, 633c280c-f0fb-49a6-bdab-4aff292ecadf:1, 94d737a0-21b6-490e-a7aa-abc252d5c26e:1, challenge:1, exhibit/text:2, input/expression:1, input/graph:2, multiple-choice:1 |

## CL Scripts

## Script 1: Screen 1 graph1 (input/graph)

```cl
throughRedGate = graph1.number("I_1") = 1

correct: graph1.number("I_c") = 1

narration(1): " 그래프(격자나 눈금이 표시되지 않은 좌표평면)에는 직선, 직선 위의 움직일 수 있는 점, 게이트를 표현하는 두 개의 빨간색 원이 표시되어 있습니다. 직선의 기울기는 양수이며 원점을 지납니다. 원은 제 이사분면에 있습니다."
narration(2):
	when throughRedGate "직선이 빨간색 게이트 사이를 지납니다."
    otherwise "직선이 빨간색 게이트 사이를 지나지 않습니다."
    

```

## Script 2: Screen 2 graph2 (input/graph)

```cl
throughRedGate = graph2.number("I_1") = 1
throughBlueGate = graph2.number("I_2") = 1

correct: graph2.number("I_c") = 1

narration(1): "그래프(격자나 눈금이 표시되지 않은 좌표평면)에는 직선, 직선 위의 움직일 수 있는 점, 게이트를 표현하는 두 개의 빨간색 원, 파란색 게이트를 표현하는 두 개의 파란색 사각형이 있습니다. 직선의 기울기는 양수이며 원점을 지납니다. 빨간색 원은 양수의 y 값을 가지며 파란색 게이트는 양수의 x 값을 가집니다."
narration(2):
	when throughRedGate and throughBlueGate "직선이 빨간색 게이트와 파란색 게이트 모두를 지납니다."
    when throughRedGate "직선이 빨간색 게이트를 지나지만 파란색 게이트는 지나지 않습니다."
    when throughBlueGate "직선이 파란색 게이트를 지나지만 빨간색 게이트는 지나지 않습니다."
    otherwise "직선이 빨간색 게이트와 파란색 게이트를 지나지 않습니다."


```

## Script 3: Screen 3 c1graph (input/graph)

```cl
#직선을 정의합니다.

number("m"): slopeOfLine(xyLine(c1input.latex))
number("b"): yInterceptOfLine(xyLine(c1input.latex))
number("x_0"): xInterceptOfLine(xyLine(c1input.latex))

#직선이 수직이라면 l = 0, 수직이 아니라면 l = 1 

number("I"): when xyLine(c1input.latex).isVertical 0 otherwise 1

#애니메이션을 실행합니다.

number("t_s"): c1input.timeSinceSubmit

readOnly: true

#그래프 내레이션 세부 사항

throughRedGate = c1graph.number("I_1") = 1
submitted = c1input.submitted
isALine = not(c1note.script.showWarning)
notALine = c1note.script.showWarning

narration(1): "빨간색 게이트는 `(0, -3)`과 `(0, -1)`에 있습니다."

narration(2):
	when submitted and isALine and throughRedGate "직선이 빨간색 게이트 사이를 지납니다."
    when submitted and isALine "직선이 빨간색 게이트 사이를 지나지 않습니다."
    otherwise ""

narration(3):
  when notALine "직선의 방정식을 입력해야 합니다. `y=x+1`와 같이 입력해 보세요."
  otherwise ""
```

## Script 4: Screen 3 c1note (exhibit/text)

```cl
#제출한 방정식이 직선의 방정식이 아니라면 학생에게 경고창을 표시합니다. 

validLine = isDefined(xyLine(c1input.latex))
showWarning = not(validLine) and c1input.submitted

message = "모든 게이트를 통과하는 직선의 방정식을 입력하세요. 방정식을 입력했으면 \"경기 시작\" 버튼을 누르세요."

warning = when showWarning "\n\n직선의 방정식을 입력해야 합니다.\n\n`y=x+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 5: Screen 3 c1input (input/expression)

```cl
#직선이 모든 게이트를 통과해야 합니다.

correct: c1graph.number("I_c") = 1

submitLabel: "경기 시작"
```

## Script 6: Screen 4 c2graph (input/graph)

```cl
#직선을 정의합니다.

number("m"): slopeOfLine(xyLine(c2input.latex))
number("b"): yInterceptOfLine(xyLine(c2input.latex))
number("x_0"): xInterceptOfLine(xyLine(c2input.latex))

#직선이 수직이라면 l = 0, 수직이 아니라면 l = 1 

number("I"): when xyLine(c2input.latex).isVertical 0 otherwise 1

#애니메이션을 실행합니다.

number("t_s"): c2input.timeSinceSubmit

readOnly: true

#그래프 내레이션 세부 사항

throughRedGate = c2graph.number("I_1") = 1 or c2graph.number("I_{1v}") = 1
throughBlueGate = c2graph.number("I_2") = 1 or c2graph.number("I_{2v}") = 1
submitted = c2input.submitted
isALine = not(c2note.script.showWarning)
notALine = c2note.script.showWarning

narration(1): "빨간색 게이트는 `(-2, 5)'와 `(2, 7)`에 있습니다. 파란색 게이트는 `(0, -1)'과 `(4, 1)`에 있습니다."

narration(2):
  when submitted and isALine and throughRedGate and throughBlueGate "직선이 빨간색 게이트와 파란색 게이트를 지납니다."
  when submitted and isALine and throughRedGate "직선이 빨간색 게이트를 지나지만 파란색 게이트는 지나지 않습니다."
  when submitted and isALine and throughBlueGate "직선이 파란색 게이트를 지나지만 빨간색 게이트는 지나지 않습니다."
  when submitted and isALine "직선이 빨간색 게이트와 파란색 게이트를 지나지 않습니다."
  otherwise ""

narration(3):
  when notALine "직선의 방정식을 입력해야 합니다. `y=x+1`와 같이 입력해 보세요."
  otherwise ""
```

## Script 7: Screen 4 c2note (exhibit/text)

```cl
#제출한 방정식이 직선의 방정식이 아니라면 학생에게 경고창을 표시합니다. 

validLine = isDefined(xyLine(c2input.latex))
showWarning = not(validLine) and c2input.submitted

message = "모든 게이트를 통과하는 직선의 방정식을 입력하세요. 방정식을 입력했으면 \"경기 시작\" 버튼을 누르세요."

warning = when showWarning "\n\n직선의 방정식을 입력해야 합니다.\n\n`y=x+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 8: Screen 4 c2input (input/expression)

```cl
#직선이 모든 게이트를 통과해야 합니다.

correct: c2graph.number("I_c") = 1

submitLabel: "경기 시작"
```

## Script 9: Screen 5 63c5ad11-bd3c-4a48-8140-e408c2276eb4 (sketch)

```cl
narration(1): "그래프(격자나 눈금이 표시되지 않은 좌표평면)에는 y 절편이 음수이고 x 절편이 양수인 직선이 표시되어 있습니다."
```

## Script 10: Screen 6 c3graph (input/graph)

```cl
#직선을 정의합니다.

number("m"): slopeOfLine(xyLine(c3input.latex))
number("b"): yInterceptOfLine(xyLine(c3input.latex))
number("x_0"): xInterceptOfLine(xyLine(c3input.latex))

#직선이 수직이라면 l = 0, 수직이 아니라면 l = 1 

number("I"): when xyLine(c3input.latex).isVertical 0 otherwise 1

#애니메이션을 실행합니다.

number("t_s"): c3input.timeSinceSubmit

readOnly: true

#그래프 내레이션 세부 사항

throughRedGate = c3graph.number("I_1") = 1
throughBlueGate = c3graph.number("I_2") = 1
throughPurpleGate = c3graph.number("I_3") = 1
submitted = c3input.submitted
isALine = not(c3note.script.showWarning)
notALine = c3note.script.showWarning

narration(1): "빨간색 게이트는 `(5.5, 7)`과 `(6, 6)`에 있습니다. 파란색 게이트는 `(0.5, 5)`와 `(1.5, 3)`에 있습니다. 보라색 게이트는 `(-6.5, 3)`과 `(-4.5, -1)`에 있습니다."

narration(2):
	when submitted and isALine and throughRedGate and throughBlueGate and throughPurpleGate "직선이 빨간색 게이트, 파란색 게이트, 보라색 게이트를 지납니다."
    when submitted and isALine and throughRedGate and throughBlueGate "직선은 빨간색 게이트와 파란색 게이트를 지나지만 보라색 게이트는 지나지 않습니다."
    when submitted and isALine and throughRedGate and throughPurpleGate "직선은 빨간색 게이트와 보라색 게이트를 지나지만 파란색 게이트는 지나지 않습니다."
    when submitted and isALine and throughBlueGate and throughPurpleGate "직선은 파란색 게이트와 보라색 게이트를 지나지만 빨간색 게이트는 지나지 않습니다."
    when submitted and isALine and throughRedGate "직선은 빨간색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
    when submitted and isALine and throughBlueGate "직선은 파란색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
    when submitted and isALine and throughPurpleGate "직선은 보라색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
    when submitted and isALine "직선이 어떤 게이트도 지나지 않습니다."
    otherwise ""

narration(3):
  when notALine "직선의 방정식을 입력해야 합니다. `y=x+1`와 같이 입력해 보세요."
  otherwise ""
```

## Script 11: Screen 6 c3note (exhibit/text)

```cl
#제출한 방정식이 직선의 방정식이 아니라면 학생에게 경고창을 표시합니다. 

validLine = isDefined(xyLine(c3input.latex))
showWarning = not(validLine) and c3input.submitted

message = "모든 게이트를 통과하는 직선의 방정식을 입력하세요. 방정식을 입력했으면 \"경기 시작\" 버튼을 누르세요."

warning = when showWarning "\n\n직선의 방정식을 입력해야 합니다.\n\n`y=x+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 12: Screen 6 c3input (input/expression)

```cl
#직선이 모든 게이트를 통과해야 합니다.

correct: c3graph.number("I_c") = 1

submitLabel: "경기 시작"
```

## Script 13: Screen 7 a39799df-ffb6-4784-8ca0-c8ac3c3560eb (sketch)

```cl
narration(1): "그래프에는 x 절편이 -2이고 y 절편이 -4인 직선이 표시되어 있습니다."
```

## Script 14: Screen 8 e1graph (input/graph)

```cl
number("n"):
	when choice10.isSelected(1) 1
	when choice10.isSelected(2) 2
	when choice10.isSelected(3) 3
	when choice10.isSelected(4) 4
    otherwise 0

narration(1): "선택한 숫자만큼 움직일 수 있는 점이 표시됩니다. 예를 들어 1을 선택하면 한 쌍의 점이 표시됩니다."
```

## Script 15: Screen 8 9315180e-8ada-4811-a975-da6cc0c991c8 (exhibit/text)

```cl
#이 값은 그래프에서 불러온 값이며 게이트의 중첩 여부를 표시합니다.

I = e1graph.number("I")

message = "이 화면에서는 직선을 이용해 풀 수 없는 스키 활강 경기 챌린지를 만들어야 합니다.

먼저 게이트의 개수를 선택해 주세요.

게이트의 수를 정했으면 움직일 수 있는 점을 드래그해 챌린지를 만들어 보세요.

왜 이 챌린지를 풀 수 없는지도 설명해 주세요."

#게이트가 중첩되었다면 학생에게 경고창을 표시합니다.

warning =
	when (I = 1) "\n\nW게이트를 서로 겹치면 안 돼요! 다시 시도해 주세요."
    otherwise ""

content: "${message}${warning}"
```

## Script 16: Screen 9 915cfcb3-1671-4e13-9ebe-958ac7c4981a (challenge)

```cl
noGates = not(ccchoice1.isSelected(1) or ccchoice1.isSelected(2) or ccchoice1.isSelected(3) or ccchoice1.isSelected(4))
stackedGates = ccgraph1.number("I") = 1
noLine = not(ccnote2.script.validLine)

#챌린지를 생성하려면 최소 하나의 게이트를 선택해야 합니다.

preventChallengeCreationReason:
	when noGates "최소 하나의 게이트를 추가해야 합니다."
    when stackedGates "게이트가 겹쳐 있으면 챌린지를 제출할 수 없습니다."
    otherwise ""

#제출된 직선이 없거나 부정확한 직선을 제출했다면 챌린지 답변을 제출할 수 없습니다.

preventSubmissionReason:
	when noLine "직선의 방정식을 입력해야 합니다."
	when not(ccgraph2.number("I_c") = 1 and ccgraph2.number("n") > 0 and ccinput2.timeSinceSubmit >= 2) "각각의 게이트를 지나는 직선의 방정식을 입력해야 합니다."
    otherwise ""
    
challengeBackground: graphLayer(ccgraph2.calculatorState)
challengeBounds: ccgraph2.bounds

responseBackground: graphLayer(ccgraph2.calculatorState)
responseBounds: ccgraph2.bounds
```

## Script 17: Screen 9 ccgraph1 (input/graph)

```cl

number("n"): n

n=
	when ccchoice1.isSelected(1) 1
	when ccchoice1.isSelected(2) 2
	when ccchoice1.isSelected(3) 3
	when ccchoice1.isSelected(4) 4
    otherwise 0

correct: ccgraph1.number("I_c") = 1

narration(1): "선택한 숫자만큼 움직일 수 있는 점이 표시됩니다. 예를 들어 1을 선택하면 한 쌍의 점이 표시됩니다."


number("p_1"): when n>=1 0 otherwise -50
number("p_2"): when n>=2 0 otherwise -50
number("p_3"): when n>=3 0 otherwise -50
number("p_4"): when n>=4 0 otherwise -50


```

## Script 18: Screen 9 16aa9f46-247b-4985-85dc-6a2da1599f41 (exhibit/text)

```cl
#이 값은 그래프에서 불러온 값이며 게이트의 중첩 여부를 표시합니다.

I = ccgraph1.number("I")

message = "몇 쌍의 게이트를 사용하고 싶으신가요?

게이트의 개수를 선택해 주세요. 선택한 후 움직일 수 있는 점을 드래그해 스키 활강 경기 챌린지를 만들어 보세요"

#게이트가 중첩되었다면 학생에게 경고창을 표시합니다.

warning =
	when (I = 1) "\n\nW게이트를 서로 겹치면 안 돼요! 다시 시도해 주세요."
    otherwise ""

content: "${message}${warning}"
```

## Script 19: Screen 9 ccchoice1 (multiple-choice)

```cl
readOnly: true
```

## Script 20: Screen 9 ccgraph2 (input/graph)

```cl
#직선을 정의합니다.

number("m"): slopeOfLine(xyLine(ccinput2.latex))
number("b"): yInterceptOfLine(xyLine(ccinput2.latex))
number("x_0"): xInterceptOfLine(xyLine(ccinput2.latex))

#직선이 수직이라면 l = 0, 수직이 아니라면 l = 1 

number("F"): when xyLine(ccinput2.latex).isVertical 0 otherwise 1

number("t_s"): ccinput2.timeSinceSubmit

number("x_{1f}"):ccgraph1.number("x_{1}")
number("x_{2f}"):ccgraph1.number("x_{2}")
number("x_{3f}"):ccgraph1.number("x_{3}")
number("x_{4f}"):ccgraph1.number("x_{4}")
number("x_{5f}"):ccgraph1.number("x_{5}")
number("x_{6f}"):ccgraph1.number("x_{6}")
number("x_{7f}"):ccgraph1.number("x_{7}")
number("x_{8f}"):ccgraph1.number("x_{8}")

number("y_{1f}"):ccgraph1.number("y_{1}")
number("y_{2f}"):ccgraph1.number("y_{2}")
number("y_{3f}"):ccgraph1.number("y_{3}")
number("y_{4f}"):ccgraph1.number("y_{4}")
number("y_{5f}"):ccgraph1.number("y_{5}")
number("y_{6f}"):ccgraph1.number("y_{6}")
number("y_{7f}"):ccgraph1.number("y_{7}")
number("y_{8f}"):ccgraph1.number("y_{8}")

gateCount =
	when ccchoice1.isSelected(1) 1
	when ccchoice1.isSelected(2) 2
	when ccchoice1.isSelected(3) 3
	when ccchoice1.isSelected(4) 4
    otherwise 0

number("n"): gateCount

readOnly: true

#그래프 내레이션 세부 사항

mainNarration = "그래프에는 ${gateCount}쌍의 도형이 표시되어 있습니다. 각각의 도형은 게이트를 의미합니다."
redPointNarration =
	when gateCount >= 1 "빨간색 원은 `(${ccgraph1.number("x_{1}")},${ccgraph1.number("y_{1}")})`와 `(${ccgraph1.number("x_{2}")},${ccgraph1.number("y_{2}")})`에 있습니다."
    otherwise ""
bluePointNarration =
	when gateCount >= 2 "파란색 사각형은 `(${ccgraph1.number("x_{3}")},${ccgraph1.number("y_{3}")})`와 `(${ccgraph1.number("x_{4}")},${ccgraph1.number("y_{4}")})`에 있습니다."
    otherwise ""
purplePointNarration =
	when gateCount >= 3 "보라색 삼각형은 `(${ccgraph1.number("x_{5}")},${ccgraph1.number("y_{5}")})`와`(${ccgraph1.number("x_{6}")},${ccgraph1.number("y_{6}")})`에 있습니다."
    otherwise ""
#blackPointNarration =
#	when gateCount >= 4 "검은색 점은 `(${ccgraph1.number("x_{7}")},${ccgraph1.number("y_{7}")})`와 `(${ccgraph1.number("x_{8}")},${ccgraph1.number("y_{8}")})`에 있습니다."
#    otherwise ""

#기본 내레이션

#narration(1): "${mainNarration}${redPointNarration}${bluePointNarration}${orangePointNarration}${blackPointNarration}"
narration(1): "${mainNarration}${redPointNarration}${bluePointNarration}${purplePointNarration}"

throughRedGate = ccgraph2.number("N_1") = 1
throughBlueGate = ccgraph2.number("N_2") = 1
throughPurpleGate = ccgraph2.number("N_3") = 1
#throughBlackGate = ccgraph2.number("N_4") = 1
submitted = ccinput2.submitted
isALine = not(ccnote2.script.showWarning)
notALine = ccnote2.script.showWarning

allGatesText = "직선이 모든 게이트를 지납니다."
noGatesText = "직선이 어떤 게이트도 지나지 않습니다."
redOnlyText = "직선은 빨간색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
blueOnlyText = "직선은 파란색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
purpleOnlyText = "직선은 보라색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
#blackOnlyText = "직선은 검은색 게이트를 지나지만 다른 게이트는 지나지 않습니다."

#gateCount = 1 일 때 피드백

narration(2):
  when gateCount = 1 and submitted and isALine and throughRedGate "${allGatesText}"
  when gateCount = 1 and submitted and isALine "직선이 빨간색 게이트를 지나지 않습니다."
  otherwise ""

#gateCount = 2 일 때 피드백

narration(3):
  when gateCount = 2 and submitted and isALine and throughRedGate and throughBlueGate "${allGatesText}"
  when gateCount = 2 and submitted and isALine and throughRedGate "직선이 빨간색 게이트를 지나지만 파란색 게이트는 지나지 않습니다."
  when gateCount = 2 and submitted and isALine and throughBlueGate "직선이 파란색 게이트를 지나지만 빨간색 게이트는 지나지 않습니다."
  when gateCount = 2 and submitted and isALine "${noGatesText}"
  otherwise ""

#gateCount = 3 일 때 피드백

narration(4):
  when gateCount = 3 and submitted and isALine and throughRedGate and throughBlueGate and throughPurpleGate "${allGatesText}"
  when gateCount = 3 and submitted and isALine and throughRedGate and throughBlueGate "직선이 빨간색 게이트와 파란색 게이트를 지나지만 보라색 게이트는 지나지 않습니다."
  when gateCount = 3 and submitted and isALine and throughRedGate and throughPurpleGate "직선이 빨간색 게이트와 보라색 게이트를 지나지만 파란색 게이트는 지나지 않습니다."
  when gateCount = 3 and submitted and isALine and throughBlueGate and throughPurpleGate "직선이 파란색 게이트와 보라색 게이트를 지나지만 빨간색 게이트는 지나지 않습니다."
  when gateCount = 3 and submitted and isALine and throughRedGate "${redOnlyText}"
  when gateCount = 3 and submitted and isALine and throughBlueGate "${blueOnlyText}"
  when gateCount = 3 and submitted and isALine and throughPurpleGate "${purpleOnlyText}"
  when gateCount = 3 and submitted and isALine "${noGatesText}"
  otherwise ""

#gateCount = 4 일 때 피드백

#narration(5):
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlueGate and throughOrangeGate and throughBlackGate "${allGatesText}"
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlueGate and throughOrangeGate "직선이 빨간색 게이트, 파란색 게이트, 주황색 게이트를 지나지만 검은색 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlueGate and throughBlackGate "직선이 빨간색 게이트, 파란색 게이트, 검은색 게이트를 지나지만 주황색 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughOrangeGate and throughBlackGate "직선이 빨간색 게이트, 주황색 게이트, 검은색 게이트를 지나지만 파란색 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughBlueGate and throughOrangeGate and throughBlackGate "직선이 파란색 게이트, 주황색 게이트, 검은색 게이트를 지나지만 빨간색 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlueGate "직선이 빨간색 게이트와 파란색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughOrangeGate "직선이 빨간색 게이트와 주황색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlackGate "직선이 빨간색 게이트와 검은색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughBlueGate and throughOrangeGate "직선이 파란색 게이트와 주황색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughBlueGate and throughBlackGate "직선이 파란색 게이트와 검은색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughOrangeGate and throughBlackGate "직선이 주황색 게이트와 검은색 게이트를 지나지만 다른 게이트는 지나지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate "${redOnlyText}"
#  when gateCount = 4 and submitted and isALine and throughBlueGate "${blueOnlyText}"
#  when gateCount = 4 and submitted and isALine and throughOrangeGate "${orangeOnlyText}"
#  when gateCount = 4 and submitted and isALine and throughBlackGate "${blackOnlyText}"
#  when gateCount = 4 and submitted and isALine "${noGatesText}"
#  otherwise ""

#학생이 직선을 입력하지 않았을 때 피드백

narration(5):
  when notALine "직선의 방정식을 입력해야 합니다. `y=x+1`와 같이 입력해 보세요."
  otherwise ""
```

## Script 21: Screen 9 ccnote2 (exhibit/text)

```cl
#제출한 방정식이 직선의 방정식이 아니라면 학생에게 경고창을 표시합니다. 

validLine = isDefined(xyLine(ccinput2.latex))
showWarning = not(validLine) and ccinput2.submitted

gateMessage = when ccchoice1.isSelected(1) "게이트" otherwise "각각의 게이트"

message = "${gateMessage}를 통과하는 직선의 방정식을 입력하세요. 방정식을 입력했으면 \"경기 시작\" 버튼을 클릭하세요."

warning = when showWarning "\n\n직선의 방정식을 입력해야 합니다.\n\n`y=x+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 22: Screen 9 ccinput2 (input/expression)

```cl
#직선이 모든 게이트를 통과해야 합니다.

correct: ccgraph2.number("n") > 0 and ccgraph2.number("I_c") = 1

submitLabel: "경기 시작"
```

