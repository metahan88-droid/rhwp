# Amplify Activity Inspection

Source URL: https://classroom.amplify.com/activity/5f36d3be6c49802c5943d5a1?lang=ko

## Static Metadata

- Activity ID: 5f36d3be6c49802c5943d5a1
- Static title: 스키 활강 경기: 포물선
- Description: 학생은 이 액티비티에서 스키 활강 경기를 테마로 한 챌린지를 통해 포물선의 방정식 및 그래프에 대한 이해도를 높일 수 있습니다.

이 액티비티는 여러 번의 수업에 걸쳐 진행해야 할 수도 있습니다. 액티비티를 나중에 이어서 진행하려면 학생이 로그인하도록 지도해 주세요.

도움 주신 분: Paul Jorgens

한국어 번역: Desmos 현지화 팀
- Canonical: https://classroom.amplify.com/activity/5f36d3be6c49802c5943d5a1
- Image: https://uploads.desmos.com/activitybuilder/d53f5e37c56641bb453fc2b745f69750

## Activity JSON Summary

- Title: 스키 활강 경기: 포물선
- Version: 1
- Commit ID: 62ff4796dcb919ce4612247a
- Published: 2022-08-19T08:36:33.971Z
- Edited: 2022-08-19T08:36:33.971Z
- Permission to share: true
- Noindex: false
- Screen count: 11

## Component Counts

- exhibit/text: 12
- input/graph: 10
- input/expression: 6
- multiple-choice: 4
- sketch: 2
- background: 2
- 17af4eb3-e206-4dc7-973e-e4555ebcd9fa: 2
- 85b749d0-447b-422b-a11a-e7e7e5fca8fe: 2
- 0a9c2a0b-6d6f-4085-99df-4ab140ba565b: 2
- 1d6563ae-8935-4d04-b2f4-f9a387bee90f: 1
- eb86ea45-d77f-4391-a43f-e1ecfbf042bf: 1
- 9f2799da-487b-4b4e-8fb0-328ef96d12e6: 1
- b2d9235a-5b06-400e-892b-a711bd672e83: 1
- challenge: 1
- 633c280c-f0fb-49a6-bdab-4aff292ecadf: 1
- 089a4959-7f91-4886-8a15-52d541bf4505: 1
- 94d737a0-21b6-490e-a7aa-abc252d5c26e: 1

## Screens

| # | Title | Type | Components |
|---:|---|---|---|
| 1 | 챌린지 준비 1 | two-column | exhibit/text:1, input/graph:1 |
| 2 | 챌린지 준비 2 | two-column | exhibit/text:1, input/graph:1 |
| 3 | 1번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 4 | 2번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 5 | 3번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 6 | 포물선의 방정식 찾기 | two-column | 0a9c2a0b-6d6f-4085-99df-4ab140ba565b:1, 17af4eb3-e206-4dc7-973e-e4555ebcd9fa:1, 85b749d0-447b-422b-a11a-e7e7e5fca8fe:1, background:1, exhibit/text:1, multiple-choice:1, sketch:1 |
| 7 | 4번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 8 | 5번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 9 | 논쟁 해결하기 | two-column | 0a9c2a0b-6d6f-4085-99df-4ab140ba565b:1, 17af4eb3-e206-4dc7-973e-e4555ebcd9fa:1, 1d6563ae-8935-4d04-b2f4-f9a387bee90f:1, 85b749d0-447b-422b-a11a-e7e7e5fca8fe:1, background:1, exhibit/text:1, multiple-choice:1, sketch:1 |
| 10 | 해결 불가능한 챌린지 만들기 | two-column | 9f2799da-487b-4b4e-8fb0-328ef96d12e6:1, b2d9235a-5b06-400e-892b-a711bd672e83:1, eb86ea45-d77f-4391-a43f-e1ecfbf042bf:1, exhibit/text:1, input/graph:1, multiple-choice:1 |
| 11 | (untitled) | two-column | 089a4959-7f91-4886-8a15-52d541bf4505:1, 633c280c-f0fb-49a6-bdab-4aff292ecadf:1, 94d737a0-21b6-490e-a7aa-abc252d5c26e:1, challenge:1, exhibit/text:2, input/expression:1, input/graph:2, multiple-choice:1 |

## CL Scripts

## Script 1: Screen 1 graph1 (input/graph)

```cl
correct: graph1.number("I_r") = 1

narration(1): "그래프에는 눈금이 없으며, 포물선 하나와 게이트를 의미하는 빨간색 원 두 개가 표시되어 있습니다. 포물선은 원점에 있는 점 A와 제 1사분면에 있는 점 B를 지납니다. 빨간색 원은 x축을 중심으로 서로 수직으로 모여 있으며, x축에 대해 서로 대칭입니다. 두 점 모두 x 값이 음수이며, 점 하나의 y 값은 양수, 다른 하나의 y 값은 음수입니다."
narration(2):
	when graph1.number("I_r") = 1 "포물선이 빨간색 게이트를 통과합니다."
    otherwise "포물선이 빨간색 게이트를 통과하지 않습니다."
```

## Script 2: Screen 2 graph2 (input/graph)

```cl
correct: graph2.number("I") = 1

narration(1): "그래프에는 눈금이 표시되어있지 않으며, 게이트를 의미하는 빨간색 원 두 개, 파란색 사각형 두 개, 보라색 삼각형 두 개가 있습니다. 포물선은 움직일 수 있는 점 A, B, C를 지나갑니다. 점 A와 B는 x축에 있으며, 점 A는 y축 왼쪽에, 점 B는 y축 오른쪽에 있습니다. 점 C는 y축에 있으며, x축 아래쪽에 있습니다. 빨간색 원은 x축에 있으며, y축 왼쪽에 있습니다. 파란색 사각형은 y축에 있으며, x축 위쪽에 있습니다. 보라색 사강형은 x축에 있으며, y축 오른쪽에 있습니다."

red=graph2.number("I_1")=1
blue=graph2.number("I_2")=1
purple=graph2.number("I_3")=1
narration(2):
	when graph2.number("I") = 1 "포물선이 모든 게이트를 통과합니다."
    when red and blue "포물선이 빨간색과 파란색 게이트를 통과하지만, 보라색 게이트는 통과하지 않습니다."
    when red and purple "포물선이 빨간색과 보라색 게이트를 통과하지만, 파란색 게이트는 통과하지 않습니다."
    when blue and purple "포물선이 파란색과 보라색 게이트는 통과하지만, 빨간색 게이트는 통과하지 않습니다."
    when red "포물선이 빨간색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
    when blue "포물선이 파란색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
    when purple "포물선이 보라색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
    
    otherwise "포물선이 어떤 게이트도 통과하지 않습니다."
```

## Script 3: Screen 3 c1graph (input/graph)

```cl
functionIsSimple = isDefined(simpleFunction(c1input.latex,"x"))
quadraticFunction = c1graph.number("P") = 1 and c1graph.number("A") = 1

function("f"): simpleFunction(c1input.latex,"x")
number("t_s"): when functionIsSimple c1input.timeSinceSubmit otherwise 0

readOnly: true

narration(1):"빨간색 게이트의 깃발은 각각 점`(0,-4)`와 `(3,-4)`에 있습니다."
narration(2): when c1graph.number("I")=1 and c1input.submitted "포물선이 빨간색 게이트를 통과합니다."
when c1input.submitted and c1graph.number("A")=1 and c1graph.number("P")=1 "포물선이 빨간색 게이트를 통과하지 않습니다."
when c1input.submitted "포물선의 방정식을 입력해야 합니다. y는 x 제곱과 같이 입력해 보세요."
otherwise ""
```

## Script 4: Screen 3 4f801c82-030c-46fa-b888-89f9e1656600 (exhibit/text)

```cl
quadraticFunction = c1graph.number("P") = 1 and c1graph.number("A") = 1
showWarning = not(quadraticFunction) and c1input.submitted

message = "게이트를 통과하는 포물선의 방정식을 입력하세요.

방정식 입력 후 <그래프 확인> 버튼을 누르세요."

warning = when showWarning "\n\n\"`y=`\"와 같은 형태로 입력해야 합니다.\n\n`y=x^2+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 5: Screen 3 c1input (input/expression)

```cl
correct: c1graph.number("I") = 1

submitLabel: "그래프 확인"
```

## Script 6: Screen 4 c2graph (input/graph)

```cl
functionIsSimple = isDefined(simpleFunction(c2input.latex,"x"))
quadraticFunction = c2graph.number("P") = 1 and c2graph.number("A") = 1

function("f"): simpleFunction(c2input.latex,"x")
number("t_s"): when functionIsSimple c2input.timeSinceSubmit otherwise 0

readOnly: true

narration(1): "빨간색 게이트의 깃발은 각각 점`(-6,1)`과 `(-5,-1)`에 있습니다. 파란색 게이트의 깃발은 각각 점 `(6,1)`과 `(5,-1)`에 있습니다."

parabola=c2graph.number("A")=1 and c2graph.number("P")=1
submitted=c2input.submitted
red=c2graph.number("I_1")=1 and parabola and submitted
blue=c2graph.number("I_2")=1 and parabola and submitted
correct=red and blue 

narration(2): when correct "포물선이 두 게이트를 모두 통과합니다."
when red "포물선이 빨간색 게이트를 통과하지만 파란색 게이트는 통과하지 않습니다."
when blue "포물선이 파란색 게이트를 통과하지만 빨간색 게이트를 통과하지 않습니다."
when submitted and parabola "포물선이 어떤 게이트도 통과하지 않습니다."
when submitted "포물선의 방정식을 입력해야 합니다. y는 x 제곱과 같이 입력해 보세요."
otherwise ""
```

## Script 7: Screen 4 31c946be-babc-420d-8eec-6880261ed9ad (exhibit/text)

```cl
quadraticFunction = c2graph.number("P") = 1 and c2graph.number("A") = 1
showWarning = not(quadraticFunction) and c2input.submitted

message = "모든 게이트를 통과하는 포물선의 방정식을 입력하세요.

방정식 입력 후 <그래프 확인> 버튼을 누르세요."

warning = when showWarning "\n\n\"`y=`\"와 같은 형태로 입력해야 합니다.\n\n`y=x^2+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 8: Screen 4 c2input (input/expression)

```cl
correct: c2graph.number("I") = 1

submitLabel: "그래프 확인"
```

## Script 9: Screen 5 c3graph (input/graph)

```cl
functionIsSimple = isDefined(simpleFunction(c3input.latex,"x"))
quadraticFunction = c3graph.number("P") = 1 and c3graph.number("A") = 1

function("f"): simpleFunction(c3input.latex,"x")
number("t_s"): when functionIsSimple c3input.timeSinceSubmit otherwise 0

readOnly: true

narration(1): "빨간색 게이트의 깃발은 각각 점 `(3,5)`와 `(4,3)`에 있습니다. 파란색 게이트의 깃발은 각각 점 `(1,-1)'과 `(2,1)`에 있습니다."

parabola=c3graph.number("A")=1 and c3graph.number("P")=1
submitted=c3input.submitted
red=c3graph.number("I_1")=1 and parabola and submitted
blue=c3graph.number("I_2")=1 and parabola and submitted
correct=red and blue 

narration(2): when correct "포물선이 모든 게이트를 통과합니다."
when red "포물선이 빨간색 게이트를 통과하지만 파란색 게이트는 통과하지 않습니다."
when blue "포물선이 파란색 게이트를 통과하지만 빨간색 게이트는 통과하지 않습니다."
when submitted and parabola "포물선이 어떤 게이트도 통과하지 않습니다."
when submitted "포물선의 방정식을 입력해야 합니다. y는 x 제곱과 같이 입력해 보세요."
otherwise ""
```

## Script 10: Screen 5 32b39675-e150-47c4-ab49-d2d16d870ae4 (exhibit/text)

```cl
quadraticFunction = c3graph.number("P") = 1 and c3graph.number("A") = 1
showWarning = not(quadraticFunction) and c3input.submitted

message = "모든 게이트를 통과하는 포물선의 방정식을 입력하세요.

방정식 입력 후 <그래프 확인> 버튼을 누르세요."

warning = when showWarning "\n\n\"`y=`\"와 같은 형태로 입력해야 합니다.\n\n`y=x^2+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 11: Screen 5 c3input (input/expression)

```cl
correct: c3graph.number("I") = 1

submitLabel: "그래프 확인"
```

## Script 12: Screen 6 e7474011-0a5f-4c3a-9184-39b49963ec9b (sketch)

```cl
narration: "그래프에는 눈금이 없으며, 아래로 볼록하고 y축으로 대칭인 포물선이 표시되어 있습니다. 포물선은 y축 아래로 평행이동한 상태입니다. 포물선은 빨간색과 파란색 게이트를 지납니다. 빨간색 게이트는 x축에 있으며, y축 왼쪽에 있습니다. 파란색 게이트는 x축에 있으며, y축 오른쪽에 있습니다."
```

## Script 13: Screen 7 c4graph (input/graph)

```cl
functionIsSimple = isDefined(simpleFunction(c4input.latex,"x"))
quadraticFunction = c4graph.number("P") = 1 and c4graph.number("A") = 1

function("f"): simpleFunction(c4input.latex,"x")
number("t_s"): when functionIsSimple c4input.timeSinceSubmit otherwise 0

readOnly: true

narration(1): "빨간색 게이트의 깃발은 각각 점 `(1,2)'와 `(3,2)`에 있습니다. 파란색 게이트의 깃발은 각각 점 `(0, 1)`과 `(0, -1)`에 있습니다. 파란색 게이트의 깃발은 각각 점 `(-5, -5)'와 `(-3, -5)`에 있습니다."

parabola=c4graph.number("A")=1 and c4graph.number("P")=1
submitted=c4input.submitted
red=c4graph.number("I_1")=1 and parabola and submitted
blue=c4graph.number("I_2")=1 and parabola and submitted
purple=c4graph.number("I_3")=1 and parabola and submitted
correct=red and blue and purple

narration(2): when correct "포물선이 모든 게이트를 통과합니다."
when red and blue "포물선이 빨간색과 파란색 게이트를 통과하지만, 보라색 게이트는 통과하지 않습니다."
when red and purple "포물선이 빨간색과 보라색 게이트를 통과하지만, 파란색 게이트는 통과하지 않습니다."
when blue and purple "포물선이 파란색과 보라색 게이트는 통과하지만, 빨간색 게이트는 통과하지 않습니다."
when red "포물선이 빨간색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
when blue "포물선이 파란색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
when purple "포물선이 보라색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
when submitted and parabola "포물선이 어떤 게이트도 통과하지 않습니다."
when submitted "포물선의 방정식을 입력해야 합니다. y는 x 제곱과 같이 입력해 보세요."
otherwise ""
```

## Script 14: Screen 7 77039d7a-f72f-4bb8-a390-6126612e0c55 (exhibit/text)

```cl
quadraticFunction = c4graph.number("P") = 1 and c4graph.number("A") = 1
showWarning = not(quadraticFunction) and c4input.submitted

message = "모든 게이트를 통과하는 포물선의 방정식을 입력하세요.

방정식 입력 후 <그래프 확인> 버튼을 누르세요."

warning = when showWarning "\n\n\"`y=`\"와 같은 형태로 입력해야 합니다.\n\n`y=x^2+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 15: Screen 7 c4input (input/expression)

```cl
correct: c4graph.number("I") = 1

submitLabel: "그래프 확인"
```

## Script 16: Screen 8 c5graph (input/graph)

```cl
functionIsSimple = isDefined(simpleFunction(c5input.latex,"x"))
quadraticFunction = c5graph.number("P") = 1 and c5graph.number("A") = 1

function("f"): simpleFunction(c5input.latex,"x")
number("t_s"): when functionIsSimple c5input.timeSinceSubmit otherwise 0

readOnly: true

narration(1): "빨간색 게이트의 깃발은 각각 점 `(-1, 2)`와 `(0, 3)`에 있습니다. 파란색 게이트의 깃발은 각각 점 `(0, -3)'과 `(1, -4)`에 있습니다. 보라색 게이트의 깃발은 각각 점 `(4, 1)'과 `(5, 2)`에 있습니다."

parabola=c5graph.number("A")=1 and c5graph.number("P")=1
submitted=c5input.submitted
red=c5graph.number("I_1")=1 and parabola and submitted
blue=c5graph.number("I_2")=1 and parabola and submitted
purple=c5graph.number("I_3")=1 and parabola and submitted
correct=red and blue and purple

narration(2): when correct "포물선이 모든 게이트를 통과합니다."
when red and blue "포물선이 빨간색과 파란색 게이트를 통과하지만, 보라색 게이트는 통과하지 않습니다."
when red and purple "포물선이 빨간색과 보라색 게이트를 통과하지만, 파란색 게이트는 통과하지 않습니다."
when blue and purple "포물선이 파란색과 보라색 게이트는 통과하지만, 빨간색 게이트는 통과하지 않습니다."
when red "포물선이 빨간색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
when blue "포물선이 파란색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
when purple "포물선이 보라색 게이트를 통과하지만, 다른 게이트는 통과하지 않습니다."
when submitted and parabola "포물선이 어떤 게이트도 통과하지 않습니다."
when submitted "포물선의 방정식을 입력해야 합니다. y는 x 제곱과 같이 입력해 보세요."
otherwise ""
```

## Script 17: Screen 8 bfe3421b-13b9-4d04-8839-43fe4ce4117e (exhibit/text)

```cl
quadraticFunction = c5graph.number("P") = 1 and c5graph.number("A") = 1
showWarning = not(quadraticFunction) and c5input.submitted

message = "모든 게이트를 통과하는 포물선의 방정식을 입력하세요.

방정식 입력 후 <그래프 확인> 버튼을 누르세요."

warning = when showWarning "\n\n\"`y=`\"와 같은 형태로 입력해야 합니다.\n\n`y=x^2+1`와 같이 입력해 보세요." otherwise ""

content: "${message}${warning}"
```

## Script 18: Screen 8 c5input (input/expression)

```cl
correct: c5graph.number("I") = 1

submitLabel: "그래프 확인"
```

## Script 19: Screen 9 0e3abe10-b7ad-4d03-8321-6fec12b52e03 (sketch)

```cl
narration: "그래프에는 포물선이 표시되어 있습니다. 포물선의 y절편은 '-5'이고, x절편은 '1'과 '5'입니다. 꼭짓점의 좌표는 '(3,4)'입니다."
```

## Script 20: Screen 10 e1graph (input/graph)

```cl
#function("f"):simpleFunction(c5input.latex,"x")
#number("t_s"):c5input.timeSinceSubmit

number("n"):
	when choice10.isSelected(1) 1
	when choice10.isSelected(2) 2
	when choice10.isSelected(3) 3
	when choice10.isSelected(4) 4
    otherwise 0

#correct: e1graph.number("I_c") = 1
n=e1graph.number("n")
narration(1): "그래프에 표시할 게이트의 숫자를 선택할 수 있으며, 선택한 수만큼 게이트가 표시됩니다."
```

## Script 21: Screen 10 4d8093be-3512-4856-8c36-6dfa7fb8b2bc (exhibit/text)

```cl
I = e1graph.number("I")

message = "이번에는 해결 불가능한 챌린지를 만들어야 합니다. 포물선으로 풀 수 없는 챌린지를 만들어 보세요.

먼저 아래에서 게이트의 숫자를 선택하세요. 그리고 게이트를 드래그해 챌린지를 만들어 보세요.

왜 이 챌린지를 풀 수 없는지도 설명해 주세요."

warning =
	when (I = 1) "\n\n게이트 깃발을 겹칠 수는 없어요. 다시 시도해 보세요."
    otherwise ""

content: "${message}${warning}"
```

## Script 22: Screen 11 ef85c1fe-e7e0-484f-969c-ae73d094ee28 (challenge)

```cl
noGates = not(ccchoice1.isSelected(1) or ccchoice1.isSelected(2) or ccchoice1.isSelected(3) or ccchoice1.isSelected(4))

preventSubmissionReason:
	when noGates "최소 1개의 게이트를 추가해야 합니다."
	when (ccgraph2.number("I_a") = 1 and ccgraph2.number("I_b") = 0) "챌린지를 제출하려면 포물선의 방정식을 입력해야 합니다."
	when not(ccgraph2.number("I_c") = 1 and ccgraph2.number("n") > 0 and ccinput2.submitted) "챌린지를 제출하려면 각각의 게이트를 통과하는 포물선을 그려야 합니다."
    otherwise ""
    
challengeBackground: graphLayer(ccgraph2.calculatorState)
challengeBounds: ccgraph2.bounds

responseBackground: graphLayer(ccgraph2.calculatorState)
responseBounds: ccgraph2.bounds
```

## Script 23: Screen 11 ccgraph1 (input/graph)

```cl
#function("f"):simpleFunction(e2input.latex,"x")
#number("t_s"):e2input.timeSinceSubmit
#number("s"):
#	when e2input.submitted 1
#    otherwise 0

number("n"):
	when ccchoice1.isSelected(1) 1
	when ccchoice1.isSelected(2) 2
	when ccchoice1.isSelected(3) 3
	when ccchoice1.isSelected(4) 4
    otherwise 0

correct: ccgraph1.number("I_c") = 1

n=ccgraph1.number("n")
narration(1): "선택한 숫자만큼의 움직일 수 있는 게이트가 추가됩니다."

number("x_3"): when this.number("n")<2 100 otherwise this.number("x_{3a}")
number("x_4"): when this.number("n")<2 100 otherwise this.number("x_{4a}")
number("x_5"): when this.number("n")<3 101 otherwise this.number("x_{5a}")
number("x_6"): when this.number("n")<3 101 otherwise this.number("x_{6a}")
number("x_7"): when this.number("n")<4 102 otherwise this.number("x_{7a}")
number("x_8"): when this.number("n")<4 102 otherwise this.number("x_{8a}")
```

## Script 24: Screen 11 16aa9f46-247b-4985-85dc-6a2da1599f41 (exhibit/text)

```cl
I = ccgraph1.number("I")

message = "활강 경기 챌린지를 만들어 보세요. 게이트의 수를 선택한 후 게이트를 드래그 하세요."

warning =
	when (I = 1) "\n\n게이트를 겹칠 수 없습니다. 다시 시도하세요."
    otherwise ""

content: "${message}${warning}"
```

## Script 25: Screen 11 ccchoice1 (multiple-choice)

```cl
readOnly: true
```

## Script 26: Screen 11 ccgraph2 (input/graph)

```cl
functionIsSimple = isDefined(simpleFunction(ccinput2.latex,"x"))
quadraticFunction = ccgraph2.number("P") = 1 and ccgraph2.number("A") = 1

number("t_s"): when functionIsSimple ccinput2.timeSinceSubmit otherwise 0

function("f"):simpleFunction(ccinput2.latex,"x")
number("s"): 1
#	when ccinput2.submitted 1
#    otherwise 0

number("x_{1f}"):ccgraph1.number("x_{1}")
number("x_{2f}"):ccgraph1.number("x_{2}")
number("x_{3f}"):ccgraph1.number("x_{3}")
number("x_{4f}"):ccgraph1.number("x_{4}")
number("x_{5f}"):ccgraph1.number("x_{5}")
number("x_{6f}"):ccgraph1.number("x_{6}")
number("x_{7f}"):ccgraph1.number("x_{7}")
number("x_{8f}"):ccgraph1.number("x_{8}")

number("y_{1f}"):ccgraph1.number("y_{1}")
number("y_{2f}"):ccgraph1.number("y_{2}")
number("y_{3f}"):ccgraph1.number("y_{3}")
number("y_{4f}"):ccgraph1.number("y_{4}")
number("y_{5f}"):ccgraph1.number("y_{5}")
number("y_{6f}"):ccgraph1.number("y_{6}")
number("y_{7f}"):ccgraph1.number("y_{7}")
number("y_{8f}"):ccgraph1.number("y_{8}")

number("n"):
	when ccchoice1.isSelected(1) 1
	when ccchoice1.isSelected(2) 2
	when ccchoice1.isSelected(3) 3
	when ccchoice1.isSelected(4) 4
    otherwise 0

readOnly: true

gateCount =
	when ccchoice1.isSelected(1) 1
	when ccchoice1.isSelected(2) 2
	when ccchoice1.isSelected(3) 3
	when ccchoice1.isSelected(4) 4
    otherwise 0

mainNarration = "그래프에는 각각의 게이트를 의미하는 ${gateCount}쌍의 도형이 표시되어 있습니다."
redPointNarration =
	when gateCount >= 1 "빨간색 원은 각각 `(${ccgraph1.number("x_{1}")},${ccgraph1.number("y_{1}")})`와 `(${ccgraph1.number("x_{2}")},${ccgraph1.number("y_{2}")})`에 있습니다."
    otherwise ""
bluePointNarration =
	when gateCount >= 2 "파란색 사각형은 각각 `(${ccgraph1.number("x_{3}")},${ccgraph1.number("y_{3}")})'와 `(${ccgraph1.number("x_{4}")},${ccgraph1.number("y_{4}")})`에 있습니다."
    otherwise ""
purplePointNarration =
	when gateCount >= 3 "보라색 삼각형은 각각 `(${ccgraph1.number("x_{5}")},${ccgraph1.number("y_{5}")})`와 `(${ccgraph1.number("x_{6}")},${ccgraph1.number("y_{6}")})`에 있습니다."
    otherwise ""
#blackPointNarration =
#    when gateCount >= 4 "검은색 점은 각각 `(${ccgraph1.number("x_{7}")},${ccgraph1.number("y_{7}")})`와 `(${ccgraph1.number("x_{8}")},${ccgraph1.number("y_{8}")})`에 있습니다."
#    otherwise ""

#Default narration.

narration(1): "${mainNarration}${redPointNarration}${bluePointNarration}${purplePointNarration}"

throughRedGate = ccgraph2.number("G_1") = 1
throughBlueGate = ccgraph2.number("G_2") = 1
throughPurpleGate = ccgraph2.number("G_3") = 1
#throughBlackGate = ccgraph2.number("G_4") = 1
submitted = ccinput2.submitted
isALine = not(ccnote2.script.showWarning)
notALine = ccnote2.script.showWarning

allGatesText = "포물선이 모든 게이트를 통과합니다."
noGatesText = "포물선이 어떤 게이트도 통과하지 않습니다."
redOnlyText = "포물선이 빨간색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
blueOnlyText = "포물선이 파란색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
purpleOnlyText = "포물선이 보라색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
#blackOnlyText = "포물선이 검은색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."

#Feedback when gateCount = 1

narration(2):
  when gateCount = 1 and submitted and isALine and throughRedGate "${allGatesText}"
  when gateCount = 1 and submitted and isALine "포물선이 빨간색 게이트를 통과하지 않습니다."
  otherwise ""

#Feedback when gateCount = 2

narration(3):
  when gateCount = 2 and submitted and isALine and throughRedGate and throughBlueGate "${allGatesText}"
  when gateCount = 2 and submitted and isALine and throughRedGate "포물선이 빨간색 게이트를 통과하지만 파란색 게이트는 통과하지 않습니다."
  when gateCount = 2 and submitted and isALine and throughBlueGate "포물선이 파란색 게이트를 통과하지만 빨간색 게이트는 통과하지 않습니다."
  when gateCount = 2 and submitted and isALine "${noGatesText}"
  otherwise ""

#Feedback when gateCount = 3

narration(4):
  when gateCount = 3 and submitted and isALine and throughRedGate and throughBlueGate and throughPurpleGate "${allGatesText}"
  when gateCount = 3 and submitted and isALine and throughRedGate and throughBlueGate "포물선이 빨간색과 파란색 게이트를 통과하지만 보라색 게이트는 통과하지 않습니다."
  when gateCount = 3 and submitted and isALine and throughRedGate and throughPurpleGate "포물선이 빨간색과 보라색 게이트를 통과하지만 파란색 게이트는 통과하지 않습니다."
  when gateCount = 3 and submitted and isALine and throughBlueGate and throughPurpleGate "포물선이 파란색과 보라색 게이트를 통과하지만 빨간색 게이트는 통과하지 않습니다."
  when gateCount = 3 and submitted and isALine and throughRedGate "${redOnlyText}"
  when gateCount = 3 and submitted and isALine and throughBlueGate "${blueOnlyText}"
  when gateCount = 3 and submitted and isALine and throughPurpleGate "${purpleOnlyText}"
  when gateCount = 3 and submitted and isALine "${noGatesText}"
  otherwise ""

#Feedback when gateCount = 4

#narration(5):
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlueGate and throughOrangeGate and throughBlackGate "${allGatesText}"
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlueGate and throughOrangeGate "포물선이 빨간색, 파란색, 주황색 게이트를 통과하지만 검은색 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlueGate and throughBlackGate "포물선이 빨간색, 파란색, 검은색 게이트를 통과하지만 주황색 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughOrangeGate and throughBlackGate "포물선이 빨간색, 주황색, 검은색 게이트를 통과하지만 파란색 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughBlueGate and throughOrangeGate and throughBlackGate "포물선이 파란색, 주황색, 검은색 게이트를 통과하지만 빨간색 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlueGate "포물선이 빨간색과 파란색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughOrangeGate "포물선이 빨간색과 주황색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate and throughBlackGate "포물선이 빨간색과 검은색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughBlueGate and throughOrangeGate "포물선이 파란색과 주황색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughBlueGate and throughBlackGate "포물선이 파란색과 검은색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughOrangeGate and throughBlackGate "포물선이 주황색과 검은색 게이트를 통과하지만 다른 게이트는 통과하지 않습니다."
#  when gateCount = 4 and submitted and isALine and throughRedGate "${redOnlyText}"
#  when gateCount = 4 and submitted and isALine and throughBlueGate "${blueOnlyText}"
#  when gateCount = 4 and submitted and isALine and throughOrangeGate "${orangeOnlyText}"
#  when gateCount = 4 and submitted and isALine and throughBlackGate "${blackOnlyText}"
#  when gateCount = 4 and submitted and isALine "${noGatesText}"
#  otherwise ""

#Feedback when student enters a non-line.

narration(5):
  when notALine "포물선의 방정식을 입력해야 합니다. y는 x 제곱과 같은 형식으로 입력해 보세요."
  otherwise ""

```

## Script 27: Screen 11 ccnote2 (exhibit/text)

```cl
quadraticFunction = ccgraph2.number("P") = 1 and ccgraph2.number("A") = 1
showWarning = not(quadraticFunction) and ccinput2.submitted

gateMessage = when ccchoice1.isSelected(1) "게이트" otherwise "각각의 게이트"

message = "모든 게이트를 통과하는 포물선의 방정식을 입력하세요.

끝났으면 <그래프 표시> 버튼을 클릭하세요."

warning = when showWarning "\n\n\"`y=`\"와 같은 형태로 입력해야 합니다.\n\n`y=x^2+1`와 같이 입력해 보세요.`." otherwise ""

content: "${message}${warning}"
```

## Script 28: Screen 11 ccinput2 (input/expression)

```cl
correct: ccgraph2.number("I_c") = 1 and ccgraph2.number("n") > 0

submitLabel: "그래프 표시"
```

