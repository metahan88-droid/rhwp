# Amplify Activity Inspection

Source URL: https://classroom.amplify.com/activity/605a526152c7243fddcf2a10?lang=ko

## Static Metadata

- Activity ID: 605a526152c7243fddcf2a10
- Static title: 점 수집
- Description: 학생은 이 액티비티에서 변수가 하나인 부등식을 이용해 최대한 많은 점을 수집하는 활동을 진행하게 됩니다.

이 액티비티는 간단한 부등식을 중심으로 진행되며 복잡한 부등식을 사용할 필요는 없습니다.

한국어 번역: Desmos 현지화 팀
- Canonical: https://classroom.amplify.com/activity/605a526152c7243fddcf2a10
- Image: https://uploads.desmos.com/activitybuilder/bd94716639f0f0c0cf6f03a41bb48bc3

## Activity JSON Summary

- Title: 점 수집
- Version: 1
- Commit ID: 630333e80da1dff397b744a3
- Published: 2022-08-22T07:46:28.220Z
- Edited: 2022-08-22T07:46:28.220Z
- Permission to share: true
- Noindex: false
- Screen count: 11

## Component Counts

- exhibit/text: 12
- input/graph: 10
- input/expression: 8
- sketch: 2
- background: 2
- multiple-choice: 2
- b88eb357-ad03-40fe-9a3b-b94bdd763b04: 1
- 05a66248-05c7-4ce3-97fa-6cc02b24aaac: 1
- 0dc66f62-b828-4cb1-98f0-37ceef56ebf8: 1
- 094a0731-fbe2-461c-9b7a-4c2897841463: 1
- f2312ff9-b86a-45f3-909e-dee04e4bb84f: 1
- 490df563-f38c-4653-869a-c986401d0180: 1
- challenge: 1
- action-button: 1

## Screens

| # | Title | Type | Components |
|---:|---|---|---|
| 1 | 1번째 준비 활동 | two-column | exhibit/text:1, input/graph:1 |
| 2 | 2번째 준비 활동 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 3 | 1번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 4 | 논쟁 해결하기 | two-column | 05a66248-05c7-4ce3-97fa-6cc02b24aaac:1, b88eb357-ad03-40fe-9a3b-b94bdd763b04:1, background:1, exhibit/text:1, multiple-choice:1, sketch:1 |
| 5 | 2번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 6 | 3번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 7 | 논쟁 해결하기 | two-column | 094a0731-fbe2-461c-9b7a-4c2897841463:1, 0dc66f62-b828-4cb1-98f0-37ceef56ebf8:1, 490df563-f38c-4653-869a-c986401d0180:1, background:1, exhibit/text:1, f2312ff9-b86a-45f3-909e-dee04e4bb84f:1, multiple-choice:1, sketch:1 |
| 8 | 4번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 9 | 5번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 10 | 6번째 챌린지 | two-column | exhibit/text:1, input/expression:1, input/graph:1 |
| 11 | (untitled) | two-column | action-button:1, challenge:1, exhibit/text:2, input/expression:1, input/graph:2 |

## CL Scripts

## Script 1: Screen 1 graph1 (input/graph)

```cl
numberList("S_1"):aggregate(graph1.number("s_1"))

correct: graph1.number("s_1") >= 9

#narration(1): "그래프에는 '0'부터 '10'까지 표시된 두 개의 평행한 수직선이 표시되어 있습니다. 각각의 수직선에는 점이 표시되어 있습니다. 상단 수직선에는 파란색 점이 표시되어 있고, 하단 수직선에는 빨간색 점이 표시되어 있습니다. '0' 지점에는 파란색 점 하나와 빨간색 점 두 개가, '1' 지점에는 파란색 점 두 개가, '2' 지점에는 파란색 점 네 개와 빨간색 점 하나가, '3' 지점에는 파란색 점 두 개와 빨간색 점 세 개가, '4' 지점에는 파란색 점 네 개와 빨간색 점 하나가, '5' 지점에는 파란색 점 하나가, '6' 지점에는 파란색 점 하나와 빨간색 점 하나가, '7' 지점에는 파란색 점 세 개와 빨간색 점 두 개가, '8' 지점에는 파란색 점 하나와 빨간색 점 세 개가, '9' 지점에는 빨간색 점 네 개가, '10' 지점에는 파란색 점 하나와 빨간색 점 세 개가 있습니다. 또한 두 개의 움직일 수 있는 점이 있습니다."
```

## Script 2: Screen 1 note1 (exhibit/text)

```cl
b_raw = graph1.number("b_1")
b = when b_raw=b_raw b_raw
otherwise 0

r_raw = graph1.number("r_1")
r = when r_raw=r_raw r_raw
otherwise 0
  
score_raw = firstDefinedValue(graph1.number("s_1"), 0)
score = when score_raw=score_raw score_raw
otherwise 0

max = graph1.number("m_1")

line1 = "최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

line4 = "(힌트: 보라색 점을 드래그하세요.)"

content: "${line1}\n\n${line2}\n\n${line3}\n\n${line4}"
```

## Script 3: Screen 2 graph2 (input/graph)

```cl

function("g"): simpleFunction("\\left\\{${input2.latex}:1,\ 0\\right\\}","x")
numberList("S_1"):aggregate(graph2.number("s_1"))

readOnly: true
```

## Script 4: Screen 2 note2 (exhibit/text)

```cl
b = firstDefinedValue(graph2.number("b_1"), 0)
r = firstDefinedValue(graph2.number("r_1"), 0)
score = firstDefinedValue(graph2.number("s_1"), 0)

countBetter = graph2.number("m_1")

line1 = "부등식을 수정해 최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

feedback =
    when (countBetter = 0) and (score > 0) "최고점을 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${line3}\n\n${feedback}"
```

## Script 5: Screen 2 input2 (input/expression)

```cl
initialLatex: "x > 2"

correct: graph2.number("s_1") >= 16
```

## Script 6: Screen 3 graph3 (input/graph)

```cl

function("g"): simpleFunction("\\left\\{${input3.latex}:1,\ 0\\right\\}","x")
numberList("S_1"):aggregate(graph3.number("s_1"))

readOnly: true
```

## Script 7: Screen 3 note3 (exhibit/text)

```cl
b = firstDefinedValue(graph3.number("b_1"), 0)
r = firstDefinedValue(graph3.number("r_1"), 0)  
score = firstDefinedValue(graph3.number("s_1"), 0)

countBetter = graph3.number("m_1")

line1 = "부등식을 입력해 최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

feedback =
    when (countBetter = 0) and (score > 0) "최고점을 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${line3}\n\n${feedback}"
```

## Script 8: Screen 3 input3 (input/expression)

```cl
correct: graph3.number("s_1") >= 16
```

## Script 9: Screen 5 graph5 (input/graph)

```cl

function("g"): simpleFunction("\\left\\{${input5.latex}:1,\ 0\\right\\}","x")
numberList("S_1"):aggregate(graph5.number("s_1"))

readOnly: true
```

## Script 10: Screen 5 note5 (exhibit/text)

```cl
b = firstDefinedValue(graph5.number("b_1"), 0)
r = firstDefinedValue(graph5.number("r_1"), 0)
score = firstDefinedValue(graph5.number("s_1"), 0)

countBetter = graph5.number("m_1")

line1 = "부등식을 입력해 최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

feedback =
    when (countBetter = 0) and (score > 0) "최고점을 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${line3}\n\n${feedback}"
```

## Script 11: Screen 5 input5 (input/expression)

```cl
correct: graph5.number("s_1") >= 16
```

## Script 12: Screen 6 graph6 (input/graph)

```cl

function("g"): simpleFunction("\\left\\{${input6.latex}:1,\ 0\\right\\}","x") 
numberList("S_1"):aggregate(graph6.number("s_1"))

readOnly: true
```

## Script 13: Screen 6 note6 (exhibit/text)

```cl
b = firstDefinedValue(graph6.number("b_1"), 0)
r = firstDefinedValue(graph6.number("r_1"), 0)
score = firstDefinedValue(graph6.number("s_1"), 0)

countBetter = graph6.number("m_1")

line1 = "부등식을 입력해 최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

feedback =
    when (countBetter = 0) and (score > 0) "최고점을 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${line3}\n\n${feedback}"
```

## Script 14: Screen 6 input6 (input/expression)

```cl
correct: graph6.number("s_1") >= 7
```

## Script 15: Screen 8 graph8 (input/graph)

```cl

function("g"): simpleFunction("\\left\\{${input8.latex}:1,\ 0\\right\\}","x") 
numberList("S_1"):aggregate(graph8.number("s_1"))

readOnly: true
```

## Script 16: Screen 8 note8 (exhibit/text)

```cl
b = firstDefinedValue(graph8.number("b_1"), 0)
r = firstDefinedValue(graph8.number("r_1"), 0)  
score = firstDefinedValue(graph8.number("s_1"), 0)

countBetter = graph8.number("m_1")

line1 = "부등식을 입력해 최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

feedback =
    when (countBetter = 0) and (score > 0) "최고점을 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${line3}\n\n${feedback}"
```

## Script 17: Screen 8 input8 (input/expression)

```cl
correct: graph8.number("s_1") >= 9
```

## Script 18: Screen 9 graph9 (input/graph)

```cl

function("g"): simpleFunction("\\left\\{${input9.latex}:1,\ 0\\right\\}","x") 
numberList("S_1"):aggregate(graph9.number("s_1"))

readOnly: true
```

## Script 19: Screen 9 note9 (exhibit/text)

```cl
b = firstDefinedValue(graph9.number("b_1"), 0)
r = firstDefinedValue(graph9.number("r_1"), 0)
score = firstDefinedValue(graph9.number("s_1"), 0)

countBetter = graph9.number("m_1")

line1 = "부등식을 입력해 최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

feedback =
    when (countBetter = 0) and (score > 0) "최고점을 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${line3}\n\n${feedback}"
```

## Script 20: Screen 9 input9 (input/expression)

```cl
correct: graph9.number("s_1") >= 9
```

## Script 21: Screen 10 graph10 (input/graph)

```cl

function("g"): simpleFunction("\\left\\{${input10.latex}:1,\ 0\\right\\}","x") 
numberList("S_1"):aggregate(graph10.number("s_1"))

readOnly: true
```

## Script 22: Screen 10 note10 (exhibit/text)

```cl
b = firstDefinedValue(graph10.number("b_1"), 0)
r = firstDefinedValue(graph10.number("r_1"), 0)
score = firstDefinedValue(graph10.number("s_1"), 0)

countBetter = graph10.number("m_1")

line1 = "부등식을 입력해 최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

feedback =
    when (countBetter = 0) and (score > 0) "최고점을 획득했습니다."
    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
    otherwise "
"

content: "${line1}\n\n${line2}\n\n${line3}\n\n${feedback}"
```

## Script 23: Screen 10 input10 (input/expression)

```cl
correct: graph10.number("s_1") >= 7
```

## Script 24: Screen 11 262ea880-d62a-4a74-bf27-2902e4e7e748 (challenge)

```cl
challengeIncomplete = not(graph12.number("I_c") = 1)
responseIncomplete = not(graph13.number("s_1") > 0)

preventChallengeCreationReason:
	when challengeIncomplete "빨간색과 파란색 점이 각각 최소 하나씩 있어야 합니다."
    otherwise ""

preventSubmissionReason:
	when responseIncomplete "점수가 양수입니다."
    otherwise ""

challengeBackground: graphLayer(graph12.calculatorState)
challengeBounds: graph12.bounds

responseBackground: graphLayer(graph13.calculatorState)
responseBounds: graph13.bounds
```

## Script 25: Screen 11 graph12 (input/graph)

```cl
number("S"): when (button12.timeSincePress = 0) 1 otherwise 0
```

## Script 26: Screen 11 button12 (action-button)

```cl
resetLabel: "점 수정"

disabled: graph12.number("I_c") = 0
```

## Script 27: Screen 11 graph13 (input/graph)

```cl
number("A_0"): graph12.number("A_0")
number("A_1"): graph12.number("A_1")
number("A_2"): graph12.number("A_2")
number("A_3"): graph12.number("A_3")
number("A_4"): graph12.number("A_4")
number("A_5"): graph12.number("A_5")
number("A_6"): graph12.number("A_6")
number("A_7"): graph12.number("A_7")
number("A_8"): graph12.number("A_8")
number("A_9"): graph12.number("A_9")
number("A_{10}"): graph12.number("A_{10}")

number("B_0"): graph12.number("B_0")
number("B_1"): graph12.number("B_1")
number("B_2"): graph12.number("B_2")
number("B_3"): graph12.number("B_3")
number("B_4"): graph12.number("B_4")
number("B_5"): graph12.number("B_5")
number("B_6"): graph12.number("B_6")
number("B_7"): graph12.number("B_7")
number("B_8"): graph12.number("B_8")
number("B_9"): graph12.number("B_9")
number("B_{10}"): graph12.number("B_{10}")

function("g"): simpleFunction("\\left\\{${input13.latex}:1,\ 0\\right\\}","x")
#numberList("S_1"):aggregate(graph13.number("s_1"))

readOnly: true
```

## Script 28: Screen 11 13fadd94-1be8-4443-b90b-074346ef78ae (exhibit/text)

```cl
b = firstDefinedValue(graph13.number("b_1"), 0)
r = firstDefinedValue(graph13.number("r_1"), 0)
score = firstDefinedValue(graph13.number("s_1"), 0)

#countBetter = graph13.number("m_1")

line1 = "부등식을 입력해 최대한 많은 점을 수집해 보세요."
redMsg =
  when r = 1 "빨간색 점 1개"
  otherwise "빨간색 점 ${r}개"
blueMsg =
  when b=1 "파란색 점 1개"
  otherwise "파란색 점 ${b}개"
line2 = "${blueMsg}와 ${redMsg}를 수집했습니다."

line3 = "점수: ${score}."

#feedback =
#    when (countBetter = 0) and (score > 0) "최고점을 획득했습니다."
#    when (countBetter > 0) "더 높은 점수를 받은 친구가 있습니다."
#    otherwise ""

#content: "${line1}\n\n${line2}\n\n${line3}\n\n${feedback}"
content: "${line1}\n\n${line2}\n\n${line3}"
```

